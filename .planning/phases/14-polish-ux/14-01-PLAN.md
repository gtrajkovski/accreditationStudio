---
phase: 14-polish-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/css/skeleton.css
  - templates/dashboard.html
  - templates/compliance/index.html
  - templates/work_queue/index.html
  - src/i18n/en-US.json
  - src/i18n/es-PR.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Skeleton loaders appear during page load instead of spinners"
    - "Skeleton layouts match final content structure (cards, lists, text)"
    - "All skeletons shimmer in sync using background-attachment: fixed"
    - "Skeleton colors adapt to light/dark theme using CSS variables"
  artifacts:
    - path: "static/css/skeleton.css"
      provides: "Synchronized skeleton loader styles with theme support"
      min_lines: 120
      exports: [".skeleton", ".skeleton-card", ".skeleton-text", ".skeleton-title", ".skeleton-avatar", ".skeleton-button", ".skeleton-badge"]
    - path: "templates/dashboard.html"
      provides: "Dashboard with skeleton placeholders for readiness ring, stats cards, and recent activity"
      contains: "skeleton skeleton-card"
    - path: "templates/compliance/index.html"
      provides: "Compliance page with skeleton placeholders for findings list"
      contains: "skeleton skeleton-text"
  key_links:
    - from: "templates/dashboard.html"
      to: "static/css/skeleton.css"
      via: "class references"
      pattern: 'class="skeleton'
    - from: "static/css/skeleton.css"
      to: "static/css/variables.css"
      via: "CSS variable references"
      pattern: "var\\(--"
---

<objective>
Replace existing spinner loading states with synchronized skeleton loaders that match final content structure and adapt to theme.

Purpose: Improve perceived performance and provide visual context during load by showing content structure instead of generic spinners.
Output: Skeleton loader CSS system with 7 variants, updated templates with skeleton placeholders for 3 key pages.
</objective>

<execution_context>
@C:/Users/gpt30/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gpt30/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-polish-ux/14-RESEARCH.md

# Existing patterns
@static/css/main.css (lines 1445-1479: current .spinner and basic .skeleton styles)
@static/css/variables.css (CSS custom properties for theme system)
@templates/base.html (base layout structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create synchronized skeleton loader CSS</name>
  <files>static/css/skeleton.css, src/i18n/en-US.json, src/i18n/es-PR.json</files>
  <action>
Create `static/css/skeleton.css` with synchronized skeleton loaders using the research-recommended pattern:

**Base skeleton class:**
- Use `background-attachment: fixed` for synchronized shimmer across all elements
- Linear gradient: `var(--bg-hover)` → `var(--bg-panel)` → `var(--accent-muted)` → `var(--bg-panel)` → `var(--bg-hover)`
- Animation: 2s ease-in-out infinite shimmer
- Border radius: `var(--radius-md)`
- Opacity: 0.7 for subtle effect

**Specific variants (7 total):**
1. `.skeleton-text`: height 1rem, width 100%, margin-bottom 0.5rem
2. `.skeleton-text-short`: height 1rem, width 60%, margin-bottom 0.5rem
3. `.skeleton-title`: height 2rem, width 40%, margin-bottom 1rem
4. `.skeleton-card`: height 120px, border-radius `var(--radius-lg)`
5. `.skeleton-avatar`: width/height 40px, border-radius 50%
6. `.skeleton-button`: height 40px, width 100px, border-radius `var(--radius-sm)`
7. `.skeleton-badge`: height 24px, width 80px, border-radius `var(--radius-full)`

**Key requirements:**
- Use ONLY existing CSS variables from variables.css (no hardcoded colors)
- Use existing animation timing variables where applicable
- Match border radius values to existing design system
- Include comment header: "/* AccreditAI - Skeleton Loaders - Phase 14 */"

**i18n strings (add to both en-US.json and es-PR.json):**
- `loading.skeleton_loading`: "Loading..." (en-US) / "Cargando..." (es-PR)
- `loading.content_loading`: "Loading content..." / "Cargando contenido..."
  </action>
  <verify>
1. File `static/css/skeleton.css` exists with 120+ lines
2. All 7 skeleton variants defined (.skeleton, .skeleton-text, .skeleton-text-short, .skeleton-title, .skeleton-card, .skeleton-avatar, .skeleton-button, .skeleton-badge)
3. No hardcoded colors - all use CSS variables (grep for `#` or `rgb` should find none)
4. `background-attachment: fixed` present in base .skeleton class
5. i18n strings added to both language files
  </verify>
  <done>
Skeleton loader CSS created with 7 variants, all using CSS variables for theme compatibility, synchronized shimmer animation using background-attachment: fixed, and i18n strings added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard with skeleton placeholders</name>
  <files>templates/dashboard.html</files>
  <action>
Update `templates/dashboard.html` to show skeleton loaders during initial page load:

**Readiness Ring section:**
- Wrap existing readiness ring in `<div id="readiness-ring-container">`
- Add skeleton placeholder: `<div class="skeleton skeleton-avatar" style="width: 200px; height: 200px;"></div>` (hidden by default, shown via JS on load)

**Stats Cards section:**
- For each stats card (.stats-grid), duplicate the structure as skeleton variant
- Use `.skeleton-card` for card outline, `.skeleton-title` for metric value, `.skeleton-text-short` for label
- Hide skeleton cards via `display: none` by default, show via `.loading` class on parent

**Recent Activity section:**
- Create skeleton list items (3 items)
- Each item: `.skeleton-avatar` (40px circle) + `.skeleton-text` + `.skeleton-text-short`
- Wrap in `<div class="skeleton-list loading">` (show only during load)

**Loading pattern:**
```html
<div class="stats-grid" id="stats-container">
  <!-- Skeleton placeholders (shown on load) -->
  <div class="skeleton-content loading">
    <div class="skeleton skeleton-card"></div>
    <div class="skeleton skeleton-card"></div>
    <div class="skeleton skeleton-card"></div>
  </div>

  <!-- Real content (shown after load) -->
  <div class="real-content" style="display: none;">
    <!-- Existing stats cards -->
  </div>
</div>
```

**JS integration (add inline script at end of template):**
```javascript
window.addEventListener('load', () => {
  document.querySelectorAll('.skeleton-content.loading').forEach(el => {
    el.classList.remove('loading');
    el.style.display = 'none';
  });
  document.querySelectorAll('.real-content').forEach(el => {
    el.style.display = '';
  });
});
```

**Important:** Match skeleton dimensions to real content to prevent layout shift (CLS). Measure existing card heights and use exact values.
  </action>
  <verify>
1. `templates/dashboard.html` contains `.skeleton-content.loading` elements
2. At least 3 sections have skeleton placeholders (readiness, stats, activity)
3. All skeleton classes reference skeleton.css variants
4. Window load event listener removes loading class and shows real content
5. No hardcoded heights that don't match real content (check against rendered page)
  </verify>
  <done>
Dashboard template updated with skeleton placeholders for readiness ring, stats cards, and recent activity. Skeleton placeholders match real content dimensions. JS removes skeletons and shows real content on window load event.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update compliance and work queue pages with skeletons</name>
  <files>templates/compliance/index.html, templates/work_queue/index.html</files>
  <action>
Apply the same skeleton loading pattern to two additional key pages:

**Compliance page (`templates/compliance/index.html`):**
- Findings list: Add skeleton list with 5 items
- Each skeleton item: `.skeleton-text` (full width for title) + `.skeleton-text-short` (60% width for description) + `.skeleton-badge` (for status)
- Wrap in `<div class="skeleton-findings loading">`
- Add window load event listener to swap skeleton for real content

**Work Queue page (`templates/work_queue/index.html`):**
- Task cards: Add skeleton cards (3 cards in grid)
- Each skeleton card: `.skeleton-title` + `.skeleton-text` (2 lines) + `.skeleton-button` (for action)
- Wrap in `<div class="skeleton-queue loading">`
- Add window load event listener to swap skeleton for real content

**Pattern consistency:**
- Use same `.loading` class approach as dashboard
- Use same window load event listener pattern
- Match skeleton dimensions to real content structure
- Include aria-label for accessibility: `aria-label="{{ t('loading.content_loading') }}"`

**Link skeleton.css in both templates:**
Add to `{% block extra_head %}`:
```html
<link rel="stylesheet" href="{{ url_for('static', filename='css/skeleton.css') }}">
```
  </action>
  <verify>
1. Both templates contain skeleton placeholders with `.loading` class
2. Both templates link to skeleton.css in `{% block extra_head %}`
3. Skeleton structures match real content (lists for findings, cards for queue)
4. Both templates include window load event listener
5. Aria labels present for accessibility
6. Visit http://localhost:5003/compliance and /work_queue - skeletons briefly visible on page load
  </verify>
  <done>
Compliance and work queue pages updated with skeleton loaders. Skeleton structures match real content layouts. All pages show skeletons during load and swap to real content on window load event.
  </done>
</task>

</tasks>

<verification>
**Visual verification:**
1. Start Flask app: `python app.py`
2. Visit dashboard (/) - observe skeleton loaders appear briefly before readiness ring and stats cards load
3. Visit /compliance - observe skeleton list items before findings render
4. Visit /work_queue - observe skeleton cards before real task cards render
5. Toggle theme (light/dark) - verify skeleton colors adapt correctly using CSS variables

**Technical verification:**
1. All skeletons shimmer in sync (background-attachment: fixed working)
2. No layout shift (CLS) when real content replaces skeletons
3. Skeleton dimensions match real content
4. No hardcoded colors in skeleton.css (all use CSS variables)
</verification>

<success_criteria>
- [ ] `static/css/skeleton.css` created with 7 skeleton variants
- [ ] All skeleton styles use CSS variables for theme compatibility
- [ ] Synchronized shimmer animation using background-attachment: fixed
- [ ] Dashboard, compliance, and work queue pages show skeleton loaders on initial load
- [ ] Skeleton layouts match final content structure (no layout shift)
- [ ] Skeletons automatically hidden and real content shown on window load event
- [ ] i18n strings added for loading states (en-US, es-PR)
- [ ] Skeleton colors adapt correctly to light/dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/14-polish-ux/14-01-SUMMARY.md` with:
- What was built (skeleton loader system)
- Key implementation decisions (background-attachment: fixed, window load listener)
- Files modified (5 files)
- How to test (visit 3 pages, toggle theme)
- Any issues encountered during implementation
</output>
