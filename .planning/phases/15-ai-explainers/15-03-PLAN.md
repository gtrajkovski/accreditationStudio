---
phase: 15-ai-explainers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/0028_chat_persistence.sql
  - src/services/chat_context_service.py
  - src/api/chat.py
  - templates/partials/chat_panel.html
  - static/js/chat.js
  - src/i18n/en-US.json
  - src/i18n/es-PR.json
  - tests/test_chat_context_service.py
autonomous: true
requirements:
  - "66"

must_haves:
  truths:
    - "User's chat messages persist across page refreshes and sessions"
    - "User sees conversation history in chat panel"
    - "User can start new conversations or continue existing ones"
    - "User sees suggested prompts after AI responses"
    - "Conversations are auto-titled from first user message"
  artifacts:
    - path: "src/db/migrations/0028_chat_persistence.sql"
      provides: "Database tables for chat conversations and messages"
      contains: "CREATE TABLE chat_conversations"
    - path: "src/services/chat_context_service.py"
      provides: "Conversation persistence and history management"
      exports: ["ChatContextService", "create_conversation", "add_message", "get_conversation_history"]
    - path: "src/api/chat.py"
      provides: "Updated chat endpoints with persistence"
      exports: ["chat_bp", "init_chat_bp"]
    - path: "templates/partials/chat_panel.html"
      provides: "Chat UI with history and conversation list"
      min_lines: 100
    - path: "static/js/chat.js"
      provides: "Frontend JS for persistent chat"
      min_lines: 200
  key_links:
    - from: "src/api/chat.py"
      to: "ChatContextService"
      via: "service method calls"
      pattern: "_chat_service\\.(create_conversation|add_message|get_conversation_history)"
    - from: "static/js/chat.js"
      to: "/api/chat"
      via: "fetch API calls"
      pattern: "fetch.*api/chat"
    - from: "src/services/chat_context_service.py"
      to: "src/db/connection.py"
      via: "database queries"
      pattern: "get_conn\\(\\)"
---

<objective>
Implement persistent chat with conversation memory, conversation list, auto-titling, and suggested follow-up prompts.

Purpose: Users lose context when refreshing the page or returning later. Persistent chat lets users continue conversations, refer back to previous AI responses, and get context-aware suggestions for next steps.

Output: ChatContextService for persistence, updated chat API with conversation management, enhanced chat UI with history sidebar and suggested prompts.
</objective>

<execution_context>
@C:/Users/gpt30/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gpt30/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ai-explainers/15-RESEARCH.md

<interfaces>
<!-- Existing chat.py structure to extend -->
From src/api/chat.py:
```python
chat_bp = Blueprint('chat', __name__)
_workspace_manager = None
_ai_client: Optional[AIClient] = None

def init_chat_bp(workspace_manager, ai_client: AIClient): ...

@chat_bp.route('/api/chat', methods=['POST'])
def chat(): ...

@chat_bp.route('/api/chat/stream', methods=['POST'])
def chat_stream(): ...
```

From src/ai/client.py:
```python
class AIClient:
    def generate(self, system: str, user: str, ...) -> str: ...
    def chat(self, message: str, system_prompt: str = None) -> str: ...
    def chat_stream(self, message: str, system_prompt: str = None) -> Generator: ...
```

From src/db/connection.py:
```python
def get_conn() -> sqlite3.Connection: ...
```

From src/core/models.py:
```python
def generate_id(prefix: str) -> str: ...
def now_iso() -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and ChatContextService</name>
  <files>src/db/migrations/0028_chat_persistence.sql, src/services/chat_context_service.py, tests/test_chat_context_service.py</files>
  <action>
1. Create migration `0028_chat_persistence.sql`:
   - Table `chat_conversations`:
     - id TEXT PRIMARY KEY
     - institution_id TEXT NOT NULL (FK to institutions)
     - user_id TEXT (nullable for single-user)
     - title TEXT (auto-generated from first message)
     - created_at TEXT NOT NULL
     - updated_at TEXT NOT NULL
     - FOREIGN KEY (institution_id) REFERENCES institutions(id) ON DELETE CASCADE
   - Table `chat_messages`:
     - id TEXT PRIMARY KEY
     - conversation_id TEXT NOT NULL (FK to chat_conversations)
     - role TEXT NOT NULL CHECK (role IN ('user', 'assistant', 'system'))
     - content TEXT NOT NULL
     - metadata JSON (for suggested prompts, evidence_refs, etc.)
     - created_at TEXT NOT NULL
     - FOREIGN KEY (conversation_id) REFERENCES chat_conversations(id) ON DELETE CASCADE
   - Index on chat_messages(conversation_id, created_at)
   - Index on chat_conversations(institution_id, updated_at DESC)

2. Create `src/services/chat_context_service.py`:
   - Class `ChatContextService` with `__init__(ai_client)` for prompt generation

   - `create_conversation(institution_id: str, user_id: str = None) -> str`:
     - Generate conv_id with generate_id("conv")
     - Insert into chat_conversations with now_iso() timestamps
     - Return conv_id

   - `add_message(conversation_id: str, role: str, content: str, metadata: dict = None)`:
     - Generate msg_id with generate_id("msg")
     - Insert into chat_messages
     - Update conversation updated_at timestamp
     - If role="user" and conversation has no title, call _auto_title()

   - `_auto_title(conversation_id: str, first_message: str)`:
     - Truncate first_message to 50 chars, clean up
     - If starts with common patterns ("Explain", "Show", "Find"), use as-is
     - Otherwise use AI to generate short title (single call, < 10 words)
     - Update conversation title

   - `get_conversation_history(conversation_id: str, limit: int = 50) -> List[Dict]`:
     - Query messages ORDER BY created_at ASC LIMIT limit
     - Return list of {"role": str, "content": str} for AIClient

   - `list_conversations(institution_id: str, limit: int = 20) -> List[Dict]`:
     - Query conversations ORDER BY updated_at DESC
     - Return list with id, title, updated_at, message_count (subquery)

   - `delete_conversation(conversation_id: str)`:
     - CASCADE delete handles messages

   - `get_conversation(conversation_id: str) -> Optional[Dict]`:
     - Return conversation metadata

3. Create `tests/test_chat_context_service.py`:
   - Test create_conversation returns valid ID
   - Test add_message persists to database
   - Test get_conversation_history returns messages in order
   - Test list_conversations returns recent first
   - Test auto-title generation from first message
   - Test delete cascades to messages
  </action>
  <verify>
    <automated>pytest tests/test_chat_context_service.py -x</automated>
  </verify>
  <done>ChatContextService persists conversations and messages, auto-generates titles, tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Updated chat API with persistence</name>
  <files>src/api/chat.py, app.py</files>
  <action>
1. Update `src/api/chat.py`:
   - Add `_chat_service: Optional[ChatContextService] = None` module variable
   - Update `init_chat_bp(workspace_manager, ai_client, chat_service)` to accept ChatContextService

   - NEW `POST /api/chat/conversations`:
     - Body: {institution_id}
     - Create new conversation, return {conversation_id, created_at}

   - NEW `GET /api/chat/conversations`:
     - Query param: institution_id
     - Return list of conversations with id, title, updated_at, message_count

   - NEW `GET /api/chat/conversations/<conversation_id>`:
     - Return conversation details + message history

   - NEW `DELETE /api/chat/conversations/<conversation_id>`:
     - Delete conversation and messages

   - UPDATE `POST /api/chat`:
     - Add optional conversation_id parameter
     - If conversation_id provided: load history, append to context
     - After response: save both user message and assistant response
     - Return {response, conversation_id}

   - UPDATE `POST /api/chat/stream`:
     - Same conversation_id handling
     - Save messages after stream completes
     - Include conversation_id in done event

   - NEW `POST /api/chat/suggestions`:
     - Body: {conversation_id, context}
     - Load recent messages
     - Call evidence_assistant_service.generate_suggested_prompts() (if available) or inline generation
     - Return {suggestions: [str]}

2. Update `app.py`:
   - Create ChatContextService(ai_client)
   - Update init_chat_bp call to include chat_service
  </action>
  <verify>
    <automated>python -c "from src.api.chat import chat_bp; from src.services.chat_context_service import ChatContextService; print('Imports OK')"</automated>
  </verify>
  <done>Chat API persists messages, supports conversation CRUD, generates suggestions.</done>
</task>

<task type="auto">
  <name>Task 3: Enhanced chat UI with history and suggestions</name>
  <files>templates/partials/chat_panel.html, static/js/chat.js, src/i18n/en-US.json, src/i18n/es-PR.json</files>
  <action>
1. Update `templates/partials/chat_panel.html`:
   - Add conversation list sidebar (collapsible on mobile):
     - "New Chat" button at top
     - List of recent conversations with title + timestamp
     - Active conversation highlighted
     - Delete button (X icon) per conversation
   - Main chat area:
     - Message history (user/assistant bubbles)
     - Input area at bottom
     - Send button
   - Suggested prompts section:
     - Row of clickable prompt chips below input
     - Auto-populate after each assistant response
     - Click to send prompt
   - Loading states: skeleton for conversation list, typing indicator for AI

2. Update `static/js/chat.js`:
   - State: currentConversationId, conversations[], messages[]

   - `loadConversations(institutionId)`:
     - GET /api/chat/conversations?institution_id=...
     - Render sidebar list

   - `createNewConversation(institutionId)`:
     - POST /api/chat/conversations
     - Update state, render empty chat

   - `loadConversation(conversationId)`:
     - GET /api/chat/conversations/{id}
     - Render message history
     - Set as current

   - `deleteConversation(conversationId)`:
     - DELETE /api/chat/conversations/{id}
     - Remove from list, clear if current

   - Update `sendMessage(message)`:
     - If no current conversation, create one first
     - Include conversation_id in POST /api/chat/stream
     - Append user message immediately
     - Stream assistant response
     - After done: fetch and display suggested prompts

   - `fetchSuggestions(conversationId, context)`:
     - POST /api/chat/suggestions
     - Render prompt chips

   - `clickSuggestion(prompt)`:
     - Set input value, trigger send

   - Initialize on page load:
     - Get institution_id from page context
     - Load conversations
     - If conversations exist, load most recent
     - Else show welcome state

3. Add i18n strings:
   - `chat.new_conversation`: "New Chat" / "Nueva Conversacion"
   - `chat.conversations`: "Conversations" / "Conversaciones"
   - `chat.no_conversations`: "No conversations yet" / "Sin conversaciones"
   - `chat.delete_confirm`: "Delete this conversation?" / "Eliminar esta conversacion?"
   - `chat.suggestions`: "Suggested" / "Sugerencias"
   - `chat.typing`: "AI is typing..." / "IA escribiendo..."
   - `chat.welcome`: "Ask me anything about compliance!" / "Preguntame sobre cumplimiento!"
  </action>
  <verify>
    <automated>python -c "import json; d = json.load(open('src/i18n/en-US.json')); assert 'chat.new_conversation' in str(d); print('i18n OK')"</automated>
  </verify>
  <done>Chat panel shows conversation sidebar, persists messages, displays suggested prompts after responses.</done>
</task>

</tasks>

<verification>
1. Migration applies: `flask db upgrade` shows 0028_chat_persistence
2. Service tests: `pytest tests/test_chat_context_service.py -v`
3. API endpoints work:
   - `curl -X POST /api/chat/conversations -d '{"institution_id":"..."}'` returns conversation_id
   - `curl /api/chat/conversations?institution_id=...` returns list
4. UI works:
   - Open chat panel, see conversation list (or welcome state if empty)
   - Click "New Chat", type message, see it persist
   - Refresh page, conversation still there with history
   - See suggested prompts after AI response
   - Click suggestion, sends that prompt
5. i18n: Both locales have all chat.* strings
</verification>

<success_criteria>
- Chat messages persist in database across page refreshes
- Conversation list shows in sidebar with recent conversations
- New Chat creates fresh conversation
- Auto-title generated from first user message
- Suggested prompts appear after each AI response
- Click suggestion sends it as message
- Delete conversation removes it and messages
- i18n strings in both locales
- Tests pass: `pytest tests/test_chat_context_service.py -x`
</success_criteria>

<output>
After completion, create `.planning/phases/15-ai-explainers/15-03-SUMMARY.md`
</output>
