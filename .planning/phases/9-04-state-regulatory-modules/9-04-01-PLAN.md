---
phase: 9-04-state-regulatory-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/models/state_regulations.py
  - src/db/migrations/0039_state_regulatory.sql
  - src/services/state_regulatory_service.py
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
must_haves:
  truths:
    - "State authorizations can be tracked with SARA status and renewal dates"
    - "Catalog requirements can be checked per state"
    - "Program licensing approvals can be recorded with exam pass rates"
    - "Per-state readiness scores can be computed"
  artifacts:
    - path: "src/core/models/state_regulations.py"
      provides: "StateAuthorization, StateCatalogRequirement, StateCatalogCompliance, StateProgramApproval dataclasses"
      exports: ["StateAuthorization", "StateCatalogRequirement", "StateCatalogCompliance", "StateProgramApproval", "StateReadinessScore", "StateSummary"]
    - path: "src/db/migrations/0039_state_regulatory.sql"
      provides: "Database tables for state regulatory tracking"
      contains: "state_authorizations"
    - path: "src/services/state_regulatory_service.py"
      provides: "Business logic for state regulatory compliance"
      exports: ["StateRegulatoryService"]
  key_links:
    - from: "src/services/state_regulatory_service.py"
      to: "src/db/connection.py"
      via: "get_conn() database queries"
      pattern: "get_conn\\(\\)"
    - from: "src/services/state_regulatory_service.py"
      to: "src/core/models/state_regulations.py"
      via: "Model imports"
      pattern: "from src.core.models.state_regulations import"
---

<objective>
Create the core backend infrastructure for state regulatory tracking.

Purpose: Enable AccreditAI to track state authorization status, catalog requirements, and program licensing approvals with computed readiness scores per state.

Output: Data models, database schema, and service layer for state regulatory compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/9-04-state-regulatory-modules/9-04-CONTEXT.md
@.planning/phases/9-04-state-regulatory-modules/9-04-RESEARCH.md
</context>

<interfaces>
<!-- Key types and patterns from the existing codebase -->

From src/core/models/helpers.py:
```python
def generate_id(prefix: str = "") -> str:
    """Generate a unique ID with optional prefix."""

def now_iso() -> str:
    """Return current UTC timestamp in ISO format."""
```

From src/core/models/standards.py (pattern reference):
```python
@dataclass
class StandardsSection:
    id: str = field(default_factory=lambda: generate_id("sec"))
    # ... fields

    def to_dict(self) -> Dict[str, Any]:
        return { ... }

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "StandardsSection":
        return cls( ... )
```

From src/services/readiness_service.py (pattern reference):
```python
@dataclass
class ReadinessScore:
    total: int
    documents: int
    compliance: int
    evidence: int
    consistency: int
    blockers: List[Blocker] = field(default_factory=list)
    breakdown: Dict[str, Any] = field(default_factory=dict)
    computed_at: str = field(default_factory=lambda: datetime.now(timezone.utc).isoformat())

    def to_dict(self) -> Dict[str, Any]:
        return { ... }

def compute_readiness(institution_id: str, accreditor_code: str = "ACCSC", conn: Optional[sqlite3.Connection] = None) -> ReadinessScore:
    """Compute complete readiness score for an institution."""
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create state_regulations.py data models</name>
  <files>src/core/models/state_regulations.py</files>
  <action>
Create `src/core/models/state_regulations.py` with 6 dataclasses following the existing codebase patterns:

1. **StateAuthorization** - Track state authorization status per institution
   - id: str (default: generate_id("stauth"))
   - institution_id: str
   - state_code: str (e.g., "CA", "TX", "NY")
   - authorization_status: str (authorized, pending, restricted, denied)
   - sara_member: bool (SARA reciprocity)
   - effective_date: Optional[str]
   - renewal_date: Optional[str]
   - contact_agency: Optional[str]
   - contact_url: Optional[str]
   - notes: Optional[str]
   - created_at: str (default: now_iso())
   - updated_at: str (default: now_iso())

2. **StateCatalogRequirement** - State-specific catalog disclosure requirements
   - id: str (default: generate_id("streq"))
   - state_code: str
   - requirement_key: str (e.g., "hours_of_operation", "refund_policy")
   - requirement_name: str
   - requirement_text: Optional[str]
   - category: str (disclosure, consumer_info, completion_rates)
   - required: bool (default: True)
   - created_at: str (default: now_iso())

3. **StateCatalogCompliance** - Institution compliance with catalog requirements
   - id: str (default: generate_id("stcomp"))
   - institution_id: str
   - state_code: str
   - requirement_id: str
   - status: str (satisfied, partial, missing)
   - evidence_doc_id: Optional[str]
   - page_reference: Optional[str]
   - notes: Optional[str]
   - created_at: str (default: now_iso())
   - updated_at: str (default: now_iso())

4. **StateProgramApproval** - Program-level licensing board approvals
   - id: str (default: generate_id("stprog"))
   - institution_id: str
   - program_id: str
   - state_code: str
   - board_name: str
   - board_url: Optional[str]
   - approved: bool (default: False)
   - approval_date: Optional[str]
   - expiration_date: Optional[str]
   - license_exam: Optional[str]
   - min_pass_rate: Optional[float]
   - current_pass_rate: Optional[float]
   - notes: Optional[str]
   - created_at: str (default: now_iso())
   - updated_at: str (default: now_iso())

5. **StateReadinessScore** - Per-state compliance score
   - state_code: str
   - total: int (0-100)
   - authorization_score: int (0-100)
   - catalog_score: int (0-100)
   - program_score: int (0-100)
   - breakdown: Dict[str, Any] (default_factory=dict)
   - computed_at: str (default: now_iso())

6. **StateSummary** - Summary view for list display
   - state_code: str
   - state_name: str
   - authorization_status: str
   - sara_member: bool
   - renewal_date: Optional[str]
   - catalog_compliance_pct: int (0-100)
   - programs_approved: int
   - programs_total: int
   - overall_score: int (0-100)

All classes must implement:
- `to_dict() -> Dict[str, Any]` method
- `@classmethod from_dict(cls, data: Dict[str, Any])` method with unknown field filtering

Use imports from src/core/models/helpers: generate_id, now_iso
Add type hints using typing: Optional, List, Dict, Any
Use dataclasses: dataclass, field
  </action>
  <verify>
    <automated>python -c "from src.core.models.state_regulations import StateAuthorization, StateCatalogRequirement, StateCatalogCompliance, StateProgramApproval, StateReadinessScore, StateSummary; a = StateAuthorization(institution_id='inst_1', state_code='CA', authorization_status='authorized', sara_member=True); print(a.to_dict())"</automated>
  </verify>
  <done>All 6 dataclasses created with to_dict/from_dict serialization, following existing codebase patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create database migration 0039_state_regulatory.sql</name>
  <files>src/db/migrations/0039_state_regulatory.sql</files>
  <action>
Create `src/db/migrations/0039_state_regulatory.sql` with 4 tables following the schema from 9-04-RESEARCH.md:

1. **state_authorizations** table:
   - id TEXT PRIMARY KEY
   - institution_id TEXT NOT NULL
   - state_code TEXT NOT NULL
   - authorization_status TEXT NOT NULL (authorized, pending, restricted, denied)
   - sara_member BOOLEAN DEFAULT FALSE
   - effective_date TEXT
   - renewal_date TEXT
   - contact_agency TEXT
   - contact_url TEXT
   - notes TEXT
   - created_at TEXT NOT NULL
   - updated_at TEXT NOT NULL
   - UNIQUE(institution_id, state_code)
   - FOREIGN KEY (institution_id) REFERENCES institutions(id) ON DELETE CASCADE

2. **state_catalog_requirements** table:
   - id TEXT PRIMARY KEY
   - state_code TEXT NOT NULL
   - requirement_key TEXT NOT NULL
   - requirement_name TEXT NOT NULL
   - requirement_text TEXT
   - category TEXT
   - required BOOLEAN DEFAULT TRUE
   - created_at TEXT NOT NULL
   - UNIQUE(state_code, requirement_key)

3. **state_catalog_compliance** table:
   - id TEXT PRIMARY KEY
   - institution_id TEXT NOT NULL
   - state_code TEXT NOT NULL
   - requirement_id TEXT NOT NULL REFERENCES state_catalog_requirements(id)
   - status TEXT NOT NULL (satisfied, partial, missing)
   - evidence_doc_id TEXT
   - page_reference TEXT
   - notes TEXT
   - created_at TEXT NOT NULL
   - updated_at TEXT NOT NULL
   - UNIQUE(institution_id, requirement_id)
   - FOREIGN KEY (institution_id) REFERENCES institutions(id) ON DELETE CASCADE

4. **state_program_approvals** table:
   - id TEXT PRIMARY KEY
   - institution_id TEXT NOT NULL
   - program_id TEXT NOT NULL
   - state_code TEXT NOT NULL
   - board_name TEXT NOT NULL
   - board_url TEXT
   - approved BOOLEAN DEFAULT FALSE
   - approval_date TEXT
   - expiration_date TEXT
   - license_exam TEXT
   - min_pass_rate REAL
   - current_pass_rate REAL
   - notes TEXT
   - created_at TEXT NOT NULL
   - updated_at TEXT NOT NULL
   - UNIQUE(institution_id, program_id, state_code)
   - FOREIGN KEY (institution_id) REFERENCES institutions(id) ON DELETE CASCADE

Add indexes for efficient queries:
- idx_state_auth_institution ON state_authorizations(institution_id)
- idx_state_auth_state ON state_authorizations(state_code)
- idx_state_catalog_req_state ON state_catalog_requirements(state_code)
- idx_state_catalog_comp_inst ON state_catalog_compliance(institution_id)
- idx_state_catalog_comp_state ON state_catalog_compliance(state_code)
- idx_state_program_inst ON state_program_approvals(institution_id)
- idx_state_program_state ON state_program_approvals(state_code)
- idx_state_program_program ON state_program_approvals(program_id)

Add header comment: "-- 0039_state_regulatory.sql" and "-- State Regulatory Compliance Tracking"
Use PRAGMA foreign_keys = ON; at the top.
  </action>
  <verify>
    <automated>python -c "from src.db.migrate import apply_migrations; result = apply_migrations(); print(f'Applied: {result}')"</automated>
  </verify>
  <done>Migration file creates all 4 tables with proper indexes and foreign keys</done>
</task>

<task type="auto">
  <name>Task 3: Create state_regulatory_service.py</name>
  <files>src/services/state_regulatory_service.py</files>
  <action>
Create `src/services/state_regulatory_service.py` as a service class with these methods:

**Class: StateRegulatoryService**

State name lookup dictionary at module level:
```python
STATE_NAMES = {
    "CA": "California", "TX": "Texas", "NY": "New York",
    "FL": "Florida", "IL": "Illinois", ...
}
```

**Authorization Methods:**
1. `add_authorization(institution_id, state_code, authorization_status, sara_member=False, effective_date=None, renewal_date=None, contact_agency=None, contact_url=None, notes=None, conn=None) -> StateAuthorization`
   - Generate ID with generate_id("stauth")
   - INSERT INTO state_authorizations
   - Return created StateAuthorization

2. `get_authorizations(institution_id, conn=None) -> List[StateAuthorization]`
   - SELECT * FROM state_authorizations WHERE institution_id = ?
   - Return list of StateAuthorization objects

3. `get_authorization(institution_id, state_code, conn=None) -> Optional[StateAuthorization]`
   - SELECT * FROM state_authorizations WHERE institution_id = ? AND state_code = ?
   - Return StateAuthorization or None

4. `update_authorization(auth_id, conn=None, **updates) -> Optional[StateAuthorization]`
   - UPDATE state_authorizations SET ... WHERE id = ?
   - Set updated_at = now_iso()
   - Return updated StateAuthorization

5. `delete_authorization(auth_id, conn=None) -> bool`
   - DELETE FROM state_authorizations WHERE id = ?
   - Return True if deleted

**Catalog Requirements Methods:**
6. `get_requirements_for_state(state_code, conn=None) -> List[StateCatalogRequirement]`
   - SELECT * FROM state_catalog_requirements WHERE state_code = ?
   - Return list of StateCatalogRequirement

7. `get_compliance_status(institution_id, state_code, conn=None) -> List[Dict]`
   - JOIN state_catalog_requirements with state_catalog_compliance
   - Return list with requirement info + compliance status

8. `update_compliance(institution_id, requirement_id, status, evidence_doc_id=None, page_reference=None, notes=None, conn=None) -> StateCatalogCompliance`
   - INSERT OR REPLACE INTO state_catalog_compliance
   - Return StateCatalogCompliance

**Program Approval Methods:**
9. `add_program_approval(institution_id, program_id, state_code, board_name, approved=False, approval_date=None, expiration_date=None, license_exam=None, min_pass_rate=None, current_pass_rate=None, board_url=None, notes=None, conn=None) -> StateProgramApproval`
   - Generate ID with generate_id("stprog")
   - INSERT INTO state_program_approvals
   - Return created StateProgramApproval

10. `get_program_approvals(institution_id, state_code=None, program_id=None, conn=None) -> List[StateProgramApproval]`
    - SELECT with optional state_code and program_id filters
    - Return list of StateProgramApproval

11. `update_program_approval(approval_id, conn=None, **updates) -> Optional[StateProgramApproval]`
    - UPDATE state_program_approvals SET ... WHERE id = ?
    - Return updated StateProgramApproval

12. `delete_program_approval(approval_id, conn=None) -> bool`
    - DELETE FROM state_program_approvals WHERE id = ?
    - Return True if deleted

**Scoring Methods:**
13. `compute_state_readiness(institution_id, state_code, conn=None) -> StateReadinessScore`
    - authorization_score: 100 if authorized, 50 if pending, 0 otherwise
    - catalog_score: (satisfied_count / total_count) * 100
    - program_score: (approved_with_passing_rates / total_programs) * 100
    - total: weighted average (authorization 30%, catalog 40%, program 30%)
    - Return StateReadinessScore with breakdown

14. `get_all_states_summary(institution_id, conn=None) -> List[StateSummary]`
    - For each state with authorization:
      - Get authorization status
      - Calculate catalog compliance %
      - Count approved programs
      - Compute overall score
    - Return list of StateSummary sorted by state_code

**Calendar Integration:**
15. `get_upcoming_renewals(institution_id, days_ahead=90, conn=None) -> List[Dict]`
    - SELECT authorizations/approvals with renewal/expiration within days_ahead
    - Return list of {type, state_code, item_name, due_date, days_until}

Follow readiness_service.py patterns for connection management (should_close pattern).
Use from src.db.connection import get_conn.
  </action>
  <verify>
    <automated>python -c "from src.services.state_regulatory_service import StateRegulatoryService; svc = StateRegulatoryService(); print('Service instantiated successfully')"</automated>
  </verify>
  <done>Service class with 15 methods for full state regulatory CRUD and scoring</done>
</task>

</tasks>

<verification>
1. All 6 dataclasses import and serialize correctly
2. Migration applies without errors
3. Service instantiates and methods are callable
4. Database tables exist with correct schema
</verification>

<success_criteria>
- src/core/models/state_regulations.py exists with 6 dataclasses
- src/db/migrations/0039_state_regulatory.sql exists and applies
- src/services/state_regulatory_service.py exists with StateRegulatoryService class
- All classes follow existing codebase patterns (to_dict/from_dict, generate_id, now_iso)
</success_criteria>

<output>
After completion, create `.planning/phases/9-04-state-regulatory-modules/9-04-01-SUMMARY.md`
</output>
