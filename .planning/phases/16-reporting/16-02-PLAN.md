---
phase: 16-reporting
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - templates/pages/reports.html
  - static/js/reports.js
  - static/css/reports.css
  - templates/base.html
  - src/i18n/en-US.json
  - src/i18n/es-PR.json
autonomous: true
requirements: [68]

must_haves:
  truths:
    - "User can view executive summary dashboard with key metrics"
    - "Dashboard displays readiness score prominently with trend indicator"
    - "Dashboard shows findings breakdown by severity in visual format"
    - "User can export dashboard view as PDF"
    - "Dashboard is accessible from main navigation"
  artifacts:
    - path: "templates/pages/reports.html"
      provides: "Reports page with executive dashboard"
      min_lines: 150
    - path: "static/js/reports.js"
      provides: "Dashboard interactions and export trigger"
      min_lines: 100
    - path: "static/css/reports.css"
      provides: "Dashboard styling"
      min_lines: 80
  key_links:
    - from: "templates/pages/reports.html"
      to: "/api/reports"
      via: "fetch calls for export"
      pattern: "fetch.*api/reports"
    - from: "templates/base.html"
      to: "templates/pages/reports.html"
      via: "navigation link"
      pattern: "href.*reports"
---

<objective>
Build executive summary dashboard UI with export capabilities.

Purpose: Provide board-ready visual overview of institution compliance status with one-click PDF export for external stakeholders and leadership presentations.

Output: Reports page with hero metrics, Chart.js visualizations, export button, and navigation integration
</objective>

<execution_context>
@C:/Users/gpt30/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gpt30/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reporting/16-01-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 16-01 -->
POST /api/reports/institutions/<institution_id>/compliance
  - Returns: { report_id, file_path, download_url }

GET /api/reports/institutions/<institution_id>
  - Returns: [{ id, report_type, title, status, file_path, generated_at }]

GET /api/reports/<report_id>/download
  - Returns: PDF file (application/pdf)

<!-- Existing readiness API -->
GET /api/readiness/institutions/<institution_id>
  - Returns: { total, documents, compliance, evidence, consistency, blockers }

<!-- Existing Chart.js usage (from portfolio comparison) -->
Chart.js 4.x already included in project
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create executive dashboard page template</name>
  <files>
    templates/pages/reports.html,
    static/css/reports.css
  </files>
  <action>
1. Create templates/pages/reports.html extending base.html:
   - Hero metrics section (top rail):
     - Overall Readiness score (large number with color: >=80 green, >=60 yellow, <60 red)
     - Trend indicator (up/down arrow with percentage vs last month)
     - 4 sub-score cards (compliance, evidence, documents, consistency)
     - Each sub-score shows value and mini progress bar

   - Charts section (2-column grid):
     - Left: Doughnut chart showing readiness breakdown (Chart.js)
     - Right: Horizontal bar chart showing findings by severity (Chart.js)

   - Report history section:
     - Table listing recent reports (date, type, status, download link)
     - "Generate Report" button that triggers PDF export

   - Export controls section:
     - "Export Executive Summary" button
     - Loading spinner while generating
     - Success/error toast notifications

2. Create static/css/reports.css:
   - Hero metrics styling (flexbox row, centered text)
   - Metric cards with shadows and rounded corners
   - Trend indicator (green for positive, red for negative)
   - Chart container sizing (400x300px each)
   - Report history table styling (consistent with other pages)
   - Export button styling (accent color, hover state)
   - Responsive breakpoints (stack on mobile)
   - Use existing CSS variables (--bg-card, --accent, --success, etc.)

Template structure:
```html
{% extends "base.html" %}
{% block content %}
<div class="reports-dashboard">
  <!-- Hero Metrics -->
  <section class="hero-metrics">
    <div class="metric-card primary">
      <div class="metric-value" id="readiness-score">--</div>
      <div class="metric-label">{{ t('reports.overall_readiness') }}</div>
      <div class="metric-trend" id="readiness-trend"></div>
    </div>
    <!-- 4 sub-score cards -->
  </section>

  <!-- Charts -->
  <section class="charts-grid">
    <div class="chart-card">
      <h3>{{ t('reports.readiness_breakdown') }}</h3>
      <canvas id="readiness-chart"></canvas>
    </div>
    <div class="chart-card">
      <h3>{{ t('reports.findings_by_severity') }}</h3>
      <canvas id="findings-chart"></canvas>
    </div>
  </section>

  <!-- Report History -->
  <section class="report-history">
    <div class="section-header">
      <h3>{{ t('reports.recent_reports') }}</h3>
      <button id="generate-report-btn" class="btn btn-accent">
        {{ t('reports.generate_report') }}
      </button>
    </div>
    <table id="reports-table">
      <!-- Populated by JS -->
    </table>
  </section>
</div>
{% endblock %}
```
  </action>
  <verify>
    <automated>python -c "from flask import Flask; app = Flask(__name__, template_folder='templates'); app.jinja_env.get_template('pages/reports.html'); print('Template valid')"</automated>
  </verify>
  <done>Reports page template renders without errors, CSS file provides responsive dashboard layout</done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard JavaScript and Chart.js visualizations</name>
  <files>
    static/js/reports.js
  </files>
  <action>
Create static/js/reports.js:

1. ReportsManager class with methods:
   - init(): Load data and initialize charts
   - loadReadiness(): Fetch /api/readiness/institutions/{id} and populate hero metrics
   - loadReportHistory(): Fetch /api/reports/institutions/{id} and populate table
   - initReadinessChart(): Create Chart.js doughnut chart
   - initFindingsChart(): Create Chart.js horizontal bar chart
   - generateReport(): POST to /api/reports/institutions/{id}/compliance
   - downloadReport(reportId): Trigger file download

2. Chart.js configurations:
   - Doughnut chart:
     ```javascript
     new Chart(ctx, {
       type: 'doughnut',
       data: {
         labels: ['Compliance', 'Evidence', 'Documents', 'Consistency'],
         datasets: [{
           data: [scores.compliance, scores.evidence, scores.documents, scores.consistency],
           backgroundColor: ['#4ade80', '#3b82f6', '#f59e0b', '#a78bfa'],
           borderWidth: 0
         }]
       },
       options: {
         responsive: true,
         cutout: '60%',
         plugins: {
           legend: { position: 'bottom' }
         }
       }
     });
     ```

   - Horizontal bar chart for findings:
     ```javascript
     new Chart(ctx, {
       type: 'bar',
       data: {
         labels: ['Critical', 'High', 'Medium', 'Low'],
         datasets: [{
           data: [findings.critical, findings.high, findings.medium, findings.low],
           backgroundColor: ['#ef4444', '#fb923c', '#fbbf24', '#4ade80']
         }]
       },
       options: {
         indexAxis: 'y',
         responsive: true
       }
     });
     ```

3. Report generation flow:
   - Show loading spinner on button
   - POST to generate endpoint
   - On success: Show toast, refresh history table, auto-download PDF
   - On error: Show error toast

4. Event listeners:
   - Generate button click
   - Download link clicks in history table
   - DOMContentLoaded to initialize

5. Trend calculation:
   - Compare current readiness to 30-day-ago readiness (if available from history)
   - Display +/- percentage with arrow icon

Use existing toast/notification pattern from other pages (if exists) or create simple toast div.
  </action>
  <verify>
    <automated>python -c "import os; assert os.path.exists('static/js/reports.js'); print('JS file exists')"</automated>
  </verify>
  <done>Chart.js visualizations render correctly, generate button triggers API call, reports table populates with download links</done>
</task>

<task type="auto">
  <name>Task 3: Add navigation link and i18n strings</name>
  <files>
    templates/base.html,
    app.py,
    src/i18n/en-US.json,
    src/i18n/es-PR.json
  </files>
  <action>
1. Update templates/base.html:
   - Add navigation link to Reports page in sidebar
   - Position after existing tools (near Compliance Heatmap or similar)
   - Use icon (document/chart icon from existing icon set)
   ```html
   <a href="{{ url_for('reports_page') }}" class="nav-link {% if request.endpoint == 'reports_page' %}active{% endif %}">
     <svg><!-- chart/report icon --></svg>
     {{ t('nav.reports') }}
   </a>
   ```

2. Add route in app.py for reports page:
   ```python
   @app.route("/reports")
   def reports_page():
       institution_id = request.args.get("institution_id", "")
       return render_template("pages/reports.html", institution_id=institution_id)
   ```

3. Update src/i18n/en-US.json with reports section:
   ```json
   "nav": {
     "reports": "Reports"
   },
   "reports": {
     "overall_readiness": "Overall Readiness",
     "readiness_breakdown": "Readiness Breakdown",
     "findings_by_severity": "Findings by Severity",
     "recent_reports": "Recent Reports",
     "generate_report": "Generate Report",
     "export_pdf": "Export PDF",
     "generating": "Generating...",
     "report_generated": "Report generated successfully",
     "download": "Download",
     "compliance_report": "Compliance Report",
     "executive_summary": "Executive Summary",
     "no_reports": "No reports generated yet",
     "trend_up": "Up from last month",
     "trend_down": "Down from last month",
     "sub_scores": {
       "compliance": "Compliance",
       "evidence": "Evidence",
       "documents": "Documents",
       "consistency": "Consistency"
     }
   }
   ```

4. Update src/i18n/es-PR.json with Spanish translations:
   ```json
   "nav": {
     "reports": "Informes"
   },
   "reports": {
     "overall_readiness": "Preparacion General",
     "readiness_breakdown": "Desglose de Preparacion",
     "findings_by_severity": "Hallazgos por Severidad",
     "recent_reports": "Informes Recientes",
     "generate_report": "Generar Informe",
     "export_pdf": "Exportar PDF",
     "generating": "Generando...",
     "report_generated": "Informe generado exitosamente",
     "download": "Descargar",
     "compliance_report": "Informe de Cumplimiento",
     "executive_summary": "Resumen Ejecutivo",
     "no_reports": "No hay informes generados",
     "trend_up": "Aumento desde el mes pasado",
     "trend_down": "Disminucion desde el mes pasado",
     "sub_scores": {
       "compliance": "Cumplimiento",
       "evidence": "Evidencia",
       "documents": "Documentos",
       "consistency": "Consistencia"
     }
   }
   ```

5. Link reports.js in reports.html template:
   ```html
   {% block scripts %}
   <script src="{{ url_for('static', filename='js/reports.js') }}"></script>
   {% endblock %}
   ```

6. Link reports.css in reports.html template:
   ```html
   {% block styles %}
   <link rel="stylesheet" href="{{ url_for('static', filename='css/reports.css') }}">
   {% endblock %}
   ```
  </action>
  <verify>
    <automated>python -c "from app import app; assert 'reports_page' in [r.endpoint for r in app.url_map.iter_rules()]; print('Route registered')"</automated>
  </verify>
  <done>Reports page accessible from navigation, i18n strings present for both locales, page route returns 200</done>
</task>

</tasks>

<verification>
1. Start Flask app: `python app.py`
2. Navigate to http://localhost:5003/reports?institution_id={id}
3. Verify hero metrics display readiness scores
4. Verify Chart.js charts render (doughnut and bar)
5. Click "Generate Report" button
6. Verify PDF downloads automatically
7. Verify report appears in history table
8. Test navigation link from sidebar
9. Switch language to es-PR and verify translations
</verification>

<success_criteria>
- Reports page accessible at /reports route
- Navigation link visible in sidebar
- Hero metrics show readiness score with color coding
- Chart.js doughnut chart displays readiness breakdown
- Chart.js bar chart displays findings by severity
- "Generate Report" button triggers PDF generation
- Report history table shows previous reports with download links
- All text uses i18n strings (English and Spanish)
</success_criteria>

<output>
After completion, create `.planning/phases/16-reporting/16-02-SUMMARY.md`
</output>
