---
phase: 9-05-enhanced-batch-processing
plan: 03
type: execute
wave: 3
depends_on:
  - 9-05-01
  - 9-05-02
files_modified:
  - src/db/migrations/0042_batch_priority.sql
  - src/services/batch_service.py
  - src/api/batch_queue_bp.py
  - templates/institutions/batch_history.html
autonomous: true
requirements:
  - BATCH-04

must_haves:
  truths:
    - "User can set priority level (critical, high, normal, low) when creating a batch"
    - "User can change priority of existing pending batches"
    - "Queue displays batches sorted by priority"
    - "User can see queue monitor card on batch history page"
    - "User can manage templates from batch history page"
  artifacts:
    - path: "src/db/migrations/0042_batch_priority.sql"
      provides: "priority_level column on batch_operations"
      contains: "ALTER TABLE batch_operations ADD COLUMN priority_level"
    - path: "src/services/batch_service.py"
      provides: "Priority support in batch creation"
      contains: "priority_level"
    - path: "src/api/batch_queue_bp.py"
      provides: "Priority change endpoint"
      contains: "priority"
    - path: "templates/institutions/batch_history.html"
      provides: "Queue monitor, priority badges, templates UI"
      contains: "Queue Monitor"
  key_links:
    - from: "templates/institutions/batch_history.html"
      to: "/api/batches/queue/status"
      via: "fetch for queue stats"
      pattern: "api/batches/queue/status"
    - from: "templates/institutions/batch_history.html"
      to: "/api/institutions/{id}/batch-templates"
      via: "fetch for templates"
      pattern: "batch-templates"
---

<objective>
Add priority queue support and enhance batch history UI with queue monitoring and templates management.

Purpose: Enable users to prioritize critical batches over routine operations and have a unified view of queue status, templates, and batch history.

Output: Priority migration, updated BatchService, priority API endpoint, enhanced batch_history.html with queue monitor and templates sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/9-05-enhanced-batch-processing/9-05-CONTEXT.md
@.planning/phases/9-05-enhanced-batch-processing/9-05-RESEARCH.md
@.planning/phases/9-05-enhanced-batch-processing/9-05-01-SUMMARY.md
@.planning/phases/9-05-enhanced-batch-processing/9-05-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts from Plans 9-05-01 and 9-05-02 -->

From src/services/batch_queue_service.py (created in 9-05-01):
```python
class BatchQueueService:
    def __init__(self, institution_id: Optional[str] = None): ...
    def get_queue_status(self) -> Dict[str, Any]: ...
    def get_recent_activity(self, limit: int = 10) -> List[Dict[str, Any]]: ...
```

From src/services/batch_template_service.py (created in 9-05-01):
```python
class BatchTemplateService:
    def __init__(self, workspace_manager): ...
    def list_templates(self, institution_id, operation_type=None, limit=50, offset=0) -> List[BatchTemplate]: ...
    def create_template(self, institution_id, name, operation_type, document_ids, description="", concurrency=1) -> BatchTemplate: ...
    def execute_template(self, template_id) -> Dict[str, Any]: ...
```

From src/services/batch_service.py (existing):
```python
class BatchService:
    def create_batch(self, institution_id, operation_type, document_ids, concurrency=3, parent_batch_id=None) -> BatchOperation: ...
    def get_batch(self, batch_id) -> Optional[BatchOperation]: ...
    def list_batches(self, institution_id, limit=20, offset=0, operation_type=None) -> List[BatchOperation]: ...
```

From src/api/batch_queue_bp.py (created in 9-05-01):
```python
batch_queue_bp = Blueprint('batch_queue', __name__)
def init_batch_queue_bp(workspace_manager): ...
# Endpoints: /api/batches/queue/status, /api/.../batch-templates/*
```

Priority levels to implement:
- 1 = critical (highest priority, immediate processing)
- 2 = high
- 3 = normal (default)
- 4 = low (lowest priority, background processing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migration for priority support</name>
  <files>src/db/migrations/0042_batch_priority.sql</files>
  <action>
Create migration file to add priority_level column to batch_operations:

```sql
-- Phase 9-05: Enhanced Batch Processing - Priority Queue
-- Migration: 0042_batch_priority.sql

-- Add priority_level column to batch_operations
-- Values: 1=critical, 2=high, 3=normal, 4=low
ALTER TABLE batch_operations ADD COLUMN priority_level INTEGER DEFAULT 3;

-- Add sla_deadline for critical batches (optional deadline timestamp)
ALTER TABLE batch_operations ADD COLUMN sla_deadline TEXT;

-- Index for priority-based queue ordering
CREATE INDEX IF NOT EXISTS idx_batch_operations_priority
ON batch_operations(status, priority_level, created_at);

-- Update existing records to have normal priority
UPDATE batch_operations SET priority_level = 3 WHERE priority_level IS NULL;
```
  </action>
  <verify>Test migration syntax: python -c "import sqlite3; conn = sqlite3.connect(':memory:'); conn.execute('CREATE TABLE batch_operations (id TEXT)'); conn.executescript(open('src/db/migrations/0042_batch_priority.sql').read()); print('Migration OK')"</verify>
  <done>Migration file exists and SQL syntax is valid</done>
</task>

<task type="auto">
  <name>Task 2: Update BatchService with priority support</name>
  <files>src/services/batch_service.py</files>
  <action>
Update BatchService to support priority in batch creation and add priority change method.

1. Update `create_batch` method signature to accept priority_level parameter:
```python
def create_batch(
    self,
    institution_id: str,
    operation_type: str,
    document_ids: List[str],
    concurrency: int = 3,
    parent_batch_id: Optional[str] = None,
    priority_level: int = 3,  # Add this parameter
) -> BatchOperation:
```

2. In `create_batch`, add priority_level to BatchOperation creation and validate:
```python
# Validate priority level (1-4)
priority_level = min(max(priority_level, 1), 4)

batch = BatchOperation(
    # ... existing fields ...
    # Note: BatchOperation dataclass needs priority_level field
)
```

3. Update `_save_batch_to_db` to include priority_level in INSERT:
```python
cursor.execute(
    """
    INSERT INTO batch_operations (
        id, institution_id, operation_type, document_count,
        completed_count, failed_count, estimated_cost, actual_cost,
        concurrency, status, created_at, started_at, completed_at,
        parent_batch_id, metadata, priority_level
    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    """,
    (
        # ... existing values ...
        batch.priority_level if hasattr(batch, 'priority_level') else 3,
    )
)
```

4. Add new method `update_priority`:
```python
def update_priority(self, batch_id: str, priority_level: int) -> Dict[str, Any]:
    """Update priority of a pending batch.

    Args:
        batch_id: Batch ID.
        priority_level: New priority (1=critical, 2=high, 3=normal, 4=low).

    Returns:
        Dict with updated batch info or error.
    """
    batch = self.get_batch(batch_id)
    if not batch:
        return {"error": "Batch not found"}

    if batch.status not in ("pending", "running"):
        return {"error": "Can only change priority of pending or running batches"}

    # Validate priority level
    priority_level = min(max(priority_level, 1), 4)

    cursor = self.conn.cursor()
    cursor.execute(
        "UPDATE batch_operations SET priority_level = ? WHERE id = ?",
        (priority_level, batch_id)
    )
    self.conn.commit()

    return {
        "batch_id": batch_id,
        "priority_level": priority_level,
        "priority_name": self._priority_name(priority_level),
    }

def _priority_name(self, level: int) -> str:
    """Convert priority level to name."""
    names = {1: "critical", 2: "high", 3: "normal", 4: "low"}
    return names.get(level, "normal")
```

5. Update `list_batches` to order by priority:
Change ORDER BY clause from `ORDER BY created_at DESC` to `ORDER BY priority_level ASC, created_at DESC` so higher priority batches appear first.

6. Also update `src/core/models/batch.py` to add priority_level field to BatchOperation dataclass:
- Add field: `priority_level: int = 3`
- Add to `to_dict()`: `"priority_level": self.priority_level,`
- Add to `from_dict()`: `priority_level=data.get("priority_level", 3),`
  </action>
  <verify>python -c "from src.services.batch_service import BatchService; print('BatchService with priority OK')"</verify>
  <done>BatchService supports priority_level in create_batch and has update_priority method</done>
</task>

<task type="auto">
  <name>Task 3: Add priority endpoint to batch_queue_bp</name>
  <files>src/api/batch_queue_bp.py</files>
  <action>
Add priority change endpoint to batch_queue_bp.

Add after the template execute endpoint:

```python
# =============================================================================
# Priority Endpoints
# =============================================================================

@batch_queue_bp.route('/api/institutions/<institution_id>/batches/<batch_id>/priority', methods=['PATCH'])
def update_batch_priority(institution_id: str, batch_id: str):
    """Update priority of a batch.

    Request Body:
        priority_level: New priority (1=critical, 2=high, 3=normal, 4=low).

    Returns:
        JSON with updated priority info.
    """
    from src.services.batch_service import BatchService

    institution = _workspace_manager.load_institution(institution_id)
    if not institution:
        return jsonify({"error": "Institution not found"}), 404

    data = request.get_json() or {}

    if 'priority_level' not in data:
        return jsonify({"error": "priority_level is required"}), 400

    try:
        priority_level = int(data['priority_level'])
    except (TypeError, ValueError):
        return jsonify({"error": "priority_level must be an integer"}), 400

    if priority_level < 1 or priority_level > 4:
        return jsonify({"error": "priority_level must be 1-4"}), 400

    batch_service = BatchService(_workspace_manager)

    # Verify batch belongs to institution
    batch = batch_service.get_batch(batch_id)
    if not batch:
        return jsonify({"error": "Batch not found"}), 404
    if batch.institution_id != institution_id:
        return jsonify({"error": "Batch does not belong to this institution"}), 403

    result = batch_service.update_priority(batch_id, priority_level)

    if "error" in result:
        return jsonify(result), 400

    return jsonify({
        "message": "Priority updated",
        **result,
    }), 200
```

Also update `get_queue_status` to include priority information in active batches. Modify BatchQueueService.get_queue_status() to include priority_level in the SELECT:

In `src/services/batch_queue_service.py`, update the active batches query:
```python
cursor.execute(f"""
    SELECT id, operation_type, document_count, completed_count, failed_count, status, created_at, priority_level
    FROM batch_operations
    {active_where}
    ORDER BY priority_level ASC, created_at ASC
""", active_params)
```
  </action>
  <verify>python -c "from src.api.batch_queue_bp import batch_queue_bp; print('batch_queue_bp with priority OK')"</verify>
  <done>Priority endpoint added and queue status includes priority info</done>
</task>

<task type="auto">
  <name>Task 4: Enhance batch_history.html with queue monitor and templates</name>
  <files>templates/institutions/batch_history.html</files>
  <action>
Enhance the batch history page with:
1. Queue Monitor card showing real-time queue status
2. Templates section for creating/managing templates
3. Priority badges in batch table
4. Priority selector in batch creation

Update the template to include these new sections:

```html
{% extends "base.html" %}
{% from 'macros/page_header.html' import page_header %}

{% block title %}Batch History - {{ institution.name }} - AccreditAI{% endblock %}

{% block content %}
{{ page_header(
    title='Batch History',
    breadcrumbs=[('Dashboard', '/dashboard'), (institution.name, '/institutions/' ~ institution.id), ('Batch History', none)],
    institution=institution,
    score=readiness_score
) }}

<!-- Queue Monitor Card -->
<div class="card mb-lg">
    <div class="card-header">
        <h3 class="card-title">Queue Monitor</h3>
    </div>
    <div class="queue-monitor-grid" id="queueMonitor">
        <div class="queue-stat">
            <div class="queue-stat-value" id="queuePending">--</div>
            <div class="queue-stat-label">Pending</div>
        </div>
        <div class="queue-stat">
            <div class="queue-stat-value" id="queueRunning">--</div>
            <div class="queue-stat-label">Running</div>
        </div>
        <div class="queue-stat">
            <div class="queue-stat-value" id="queueCompleted">--</div>
            <div class="queue-stat-label">Completed</div>
        </div>
        <div class="queue-stat">
            <div class="queue-stat-value" id="queueFailed">--</div>
            <div class="queue-stat-label">Failed</div>
        </div>
        <div class="queue-stat">
            <div class="queue-stat-value" id="queueDepth">--</div>
            <div class="queue-stat-label">Queue Depth</div>
        </div>
    </div>
    <div id="activeBatches" class="active-batches-list">
        <!-- Active batches rendered here -->
    </div>
</div>

<!-- Stats Summary Cards -->
<div class="dashboard-grid mb-lg" id="statsCards">
    <div class="card">
        <h3 class="card-title">Total Batches</h3>
        <div id="totalBatches" class="stat-value">--</div>
    </div>
    <div class="card">
        <h3 class="card-title">Documents Processed</h3>
        <div id="totalDocs" class="stat-value">--</div>
    </div>
    <div class="card">
        <h3 class="card-title">Total Cost</h3>
        <div id="totalCost" class="stat-value">--</div>
    </div>
    <div class="card">
        <h3 class="card-title">Avg Success Rate</h3>
        <div id="avgSuccess" class="stat-value">--</div>
    </div>
</div>

<!-- Templates Section -->
<div class="card mb-lg">
    <div class="card-header" style="display: flex; justify-content: space-between; align-items: center;">
        <h3 class="card-title">Batch Templates</h3>
        <button class="btn btn-secondary" onclick="showTemplateModal()">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 16px; height: 16px; margin-right: 4px;">
                <path d="M12 5v14M5 12h14"/>
            </svg>
            New Template
        </button>
    </div>
    <div id="templateList" class="template-list">
        <p class="text-muted text-center" style="padding: var(--spacing-lg);">Loading templates...</p>
    </div>
</div>

<!-- Batch List -->
<div class="card">
    <div class="card-header" style="display: flex; justify-content: space-between; align-items: center;">
        <h3 class="card-title">Batch Operations</h3>
        <div>
            <select id="typeFilter" class="form-control" style="width: auto; display: inline-block;">
                <option value="">All Types</option>
                <option value="audit">Audit</option>
                <option value="remediation">Remediation</option>
            </select>
        </div>
    </div>
    <div id="batchTableContainer">
        <table class="data-table">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Type</th>
                    <th>Priority</th>
                    <th>Documents</th>
                    <th>Success Rate</th>
                    <th>Cost</th>
                    <th>Duration</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody id="batchList">
                <tr>
                    <td colspan="9" class="text-center text-muted" style="padding: var(--spacing-xl);">
                        Loading batches...
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

<!-- Batch Detail Modal -->
<div id="batchDetailModal" class="modal hidden">
    <div class="modal-backdrop" onclick="closeDetailModal()"></div>
    <div class="modal-content" style="max-width: 800px;">
        <div class="modal-header">
            <h3 id="detailTitle">Batch Details</h3>
            <button class="btn btn-ghost" onclick="closeDetailModal()">&times;</button>
        </div>
        <div class="modal-body" id="batchDetailContent">
            <!-- Populated by JS -->
        </div>
    </div>
</div>

<!-- Template Modal -->
<div id="templateModal" class="modal hidden">
    <div class="modal-backdrop" onclick="closeTemplateModal()"></div>
    <div class="modal-content">
        <div class="modal-header">
            <h3>New Template</h3>
            <button class="btn btn-ghost" onclick="closeTemplateModal()">&times;</button>
        </div>
        <div class="modal-body">
            <form id="templateForm" onsubmit="saveTemplate(event)">
                <div class="form-group">
                    <label for="templateName">Name</label>
                    <input type="text" id="templateName" class="form-control" required placeholder="e.g., Weekly Catalog Audit">
                </div>
                <div class="form-group">
                    <label for="templateType">Operation Type</label>
                    <select id="templateType" class="form-control" required>
                        <option value="audit">Audit</option>
                        <option value="remediation">Remediation</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="templateDocs">Documents</label>
                    <select id="templateDocs" class="form-control" multiple required style="height: 150px;">
                        <!-- Populated by JS -->
                    </select>
                    <small class="text-muted">Hold Ctrl/Cmd to select multiple</small>
                </div>
                <div class="form-group">
                    <label for="templateConcurrency">Concurrency</label>
                    <select id="templateConcurrency" class="form-control">
                        <option value="1">1 (Sequential)</option>
                        <option value="2">2</option>
                        <option value="3" selected>3</option>
                        <option value="4">4</option>
                        <option value="5">5 (Maximum)</option>
                    </select>
                </div>
                <div class="form-actions">
                    <button type="button" class="btn btn-secondary" onclick="closeTemplateModal()">Cancel</button>
                    <button type="submit" class="btn btn-primary">Save Template</button>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
.queue-monitor-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: var(--spacing-md);
    padding: var(--spacing-md);
}

.queue-stat {
    text-align: center;
    padding: var(--spacing-md);
    background: var(--bg-secondary);
    border-radius: var(--radius-md);
}

.queue-stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--accent-gold);
}

.queue-stat-label {
    font-size: 0.85em;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.active-batches-list {
    padding: var(--spacing-md);
    border-top: 1px solid var(--border-subtle);
}

.active-batch-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-sm);
    background: var(--bg-secondary);
    border-radius: var(--radius-sm);
    margin-bottom: var(--spacing-xs);
}

.active-batch-item .progress-bar {
    flex: 1;
    height: 8px;
    background: var(--bg-primary);
    border-radius: 4px;
    overflow: hidden;
}

.active-batch-item .progress-fill {
    height: 100%;
    background: var(--accent-gold);
    transition: width 0.3s;
}

.template-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--spacing-md);
    padding: var(--spacing-md);
}

.template-card {
    background: var(--bg-secondary);
    border-radius: var(--radius-md);
    padding: var(--spacing-md);
    border: 1px solid var(--border-subtle);
}

.template-card h4 {
    margin: 0 0 var(--spacing-xs) 0;
}

.template-card .template-meta {
    font-size: 0.85em;
    color: var(--text-muted);
    margin-bottom: var(--spacing-md);
}

.template-card .template-actions {
    display: flex;
    gap: var(--spacing-xs);
}

.priority-badge {
    display: inline-block;
    padding: 2px 8px;
    border-radius: var(--radius-sm);
    font-size: 0.75em;
    font-weight: 600;
    text-transform: uppercase;
}

.priority-critical {
    background: rgba(239, 68, 68, 0.2);
    color: #ef4444;
}

.priority-high {
    background: rgba(251, 191, 36, 0.2);
    color: #fbbf24;
}

.priority-normal {
    background: rgba(156, 163, 175, 0.2);
    color: #9ca3af;
}

.priority-low {
    background: rgba(107, 114, 128, 0.2);
    color: #6b7280;
}

.stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--accent-gold);
    text-align: center;
    padding: var(--spacing-md);
}

.data-table {
    width: 100%;
    border-collapse: collapse;
}

.data-table thead {
    background: var(--bg-secondary);
    border-bottom: 2px solid var(--border-color);
}

.data-table th {
    padding: var(--spacing-sm) var(--spacing-md);
    text-align: left;
    font-weight: 600;
    font-size: 0.9em;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--text-secondary);
}

.data-table tbody tr {
    border-bottom: 1px solid var(--border-subtle);
    cursor: pointer;
    transition: background 0.2s;
}

.data-table tbody tr:hover {
    background: var(--bg-secondary);
}

.data-table td {
    padding: var(--spacing-md);
}

.batch-type-badge {
    display: inline-block;
    padding: var(--spacing-xs) var(--spacing-sm);
    border-radius: var(--radius-sm);
    font-size: 0.85em;
    font-weight: 500;
    text-transform: uppercase;
}

.badge-audit {
    background: rgba(168, 85, 247, 0.2);
    color: #a855f7;
}

.badge-remediation {
    background: rgba(59, 130, 246, 0.2);
    color: #3b82f6;
}

.item-list {
    max-height: 400px;
    overflow-y: auto;
}

.item-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-sm) var(--spacing-md);
    border-bottom: 1px solid var(--border-subtle);
}

.item-row:last-child {
    border-bottom: none;
}

.item-name {
    font-weight: 500;
    flex: 1;
}

.item-stats {
    display: flex;
    gap: var(--spacing-md);
    font-size: 0.85em;
    color: var(--text-muted);
}

.modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: var(--z-modal, 1300);
    display: flex;
    align-items: center;
    justify-content: center;
}

.modal.hidden {
    display: none;
}

.modal-backdrop {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
}

.modal-content {
    position: relative;
    background: var(--bg-card);
    border-radius: var(--radius-lg);
    width: 90%;
    max-width: 600px;
    box-shadow: var(--shadow-lg);
    max-height: 90vh;
    overflow-y: auto;
}

.modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-md) var(--spacing-lg);
    border-bottom: 1px solid var(--border-color);
}

.modal-header h3 {
    margin: 0;
}

.modal-body {
    padding: var(--spacing-lg);
}

.form-control {
    width: 100%;
    padding: var(--spacing-sm);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    background: var(--bg-secondary);
    color: var(--text-primary);
}

.form-group {
    margin-bottom: var(--spacing-md);
}

.form-group label {
    display: block;
    margin-bottom: var(--spacing-xs);
    font-weight: 500;
}

.form-actions {
    display: flex;
    justify-content: flex-end;
    gap: var(--spacing-sm);
    margin-top: var(--spacing-lg);
}

.summary-section {
    margin-bottom: var(--spacing-lg);
}

.summary-section h4 {
    margin-bottom: var(--spacing-sm);
    color: var(--text-secondary);
    font-size: 0.9em;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.summary-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-md);
}

.summary-item {
    display: flex;
    justify-content: space-between;
    padding: var(--spacing-sm);
    background: var(--bg-secondary);
    border-radius: var(--radius-sm);
}

.summary-label {
    color: var(--text-muted);
}

.summary-value {
    font-weight: 600;
}
</style>
{% endblock %}

{% block extra_scripts %}
<script>
const institutionId = '{{ institution.id }}';
const priorityNames = {1: 'critical', 2: 'high', 3: 'normal', 4: 'low'};

// Queue Monitor
async function loadQueueStatus() {
    try {
        const res = await fetch(`/api/batches/queue/status?institution_id=${institutionId}`);
        if (!res.ok) throw new Error('Failed to load queue status');
        const data = await res.json();

        document.getElementById('queuePending').textContent = data.status_counts?.pending || 0;
        document.getElementById('queueRunning').textContent = data.status_counts?.running || 0;
        document.getElementById('queueCompleted').textContent = data.status_counts?.completed || 0;
        document.getElementById('queueFailed').textContent = data.status_counts?.failed || 0;
        document.getElementById('queueDepth').textContent = data.queue_depth || 0;

        // Render active batches
        renderActiveBatches(data.active_batches || []);
    } catch (err) {
        console.error('Failed to load queue status:', err);
    }
}

function renderActiveBatches(batches) {
    const container = document.getElementById('activeBatches');
    if (batches.length === 0) {
        container.innerHTML = '<p class="text-muted text-center">No active batches</p>';
        return;
    }

    container.innerHTML = batches.map(b => {
        const progress = b.document_count > 0
            ? ((b.completed_count + b.failed_count) / b.document_count * 100).toFixed(0)
            : 0;
        const priorityName = priorityNames[b.priority_level] || 'normal';

        return `
            <div class="active-batch-item">
                <span class="priority-badge priority-${priorityName}">${priorityName}</span>
                <span>${b.operation_type}</span>
                <div class="progress-bar">
                    <div class="progress-fill" style="width: ${progress}%"></div>
                </div>
                <span>${progress}%</span>
            </div>
        `;
    }).join('');
}

// Templates
async function loadTemplates() {
    try {
        const res = await fetch(`/api/institutions/${institutionId}/batch-templates`);
        if (!res.ok) throw new Error('Failed to load templates');
        const data = await res.json();
        renderTemplates(data.templates || []);
    } catch (err) {
        console.error('Failed to load templates:', err);
        document.getElementById('templateList').innerHTML =
            '<p class="text-muted text-center">Failed to load templates</p>';
    }
}

function renderTemplates(templates) {
    const container = document.getElementById('templateList');
    if (templates.length === 0) {
        container.innerHTML = '<p class="text-muted text-center" style="grid-column: 1/-1;">No templates yet. Create one to save batch configurations for reuse.</p>';
        return;
    }

    container.innerHTML = templates.map(t => `
        <div class="template-card">
            <h4>${escapeHtml(t.name)}</h4>
            <div class="template-meta">
                <span class="batch-type-badge badge-${t.operation_type}">${t.operation_type}</span>
                <span>${t.document_ids.length} documents</span>
            </div>
            <div class="template-actions">
                <button class="btn btn-sm btn-primary" onclick="executeTemplate('${t.id}')">Run</button>
                <button class="btn btn-sm btn-danger" onclick="deleteTemplate('${t.id}')">Delete</button>
            </div>
        </div>
    `).join('');
}

function showTemplateModal() {
    loadDocumentsForTemplate();
    document.getElementById('templateModal').classList.remove('hidden');
}

function closeTemplateModal() {
    document.getElementById('templateModal').classList.add('hidden');
}

async function loadDocumentsForTemplate() {
    try {
        const res = await fetch(`/api/institutions/${institutionId}/documents`);
        if (!res.ok) return;
        const data = await res.json();

        const select = document.getElementById('templateDocs');
        select.innerHTML = (data.documents || []).map(d =>
            `<option value="${d.id}">${escapeHtml(d.title || d.name)}</option>`
        ).join('');
    } catch (err) {
        console.error('Failed to load documents:', err);
    }
}

async function saveTemplate(e) {
    e.preventDefault();

    const docSelect = document.getElementById('templateDocs');
    const selectedDocs = Array.from(docSelect.selectedOptions).map(o => o.value);

    if (selectedDocs.length === 0) {
        window.toast?.error('Select at least one document');
        return;
    }

    const data = {
        name: document.getElementById('templateName').value,
        operation_type: document.getElementById('templateType').value,
        document_ids: selectedDocs,
        concurrency: parseInt(document.getElementById('templateConcurrency').value),
    };

    try {
        const res = await fetch(`/api/institutions/${institutionId}/batch-templates`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(data),
        });

        if (!res.ok) {
            const err = await res.json();
            throw new Error(err.error || 'Failed to create template');
        }

        closeTemplateModal();
        document.getElementById('templateForm').reset();
        loadTemplates();
        window.toast?.success('Template created');
    } catch (err) {
        window.toast?.error(err.message);
    }
}

async function executeTemplate(id) {
    try {
        const res = await fetch(`/api/institutions/${institutionId}/batch-templates/${id}/execute`, { method: 'POST' });
        if (!res.ok) {
            const err = await res.json();
            throw new Error(err.error || 'Failed to execute template');
        }
        const data = await res.json();
        loadQueueStatus();
        loadBatches();
        window.toast?.success(`Batch ${data.batch_id} created`);
    } catch (err) {
        window.toast?.error(err.message);
    }
}

async function deleteTemplate(id) {
    if (!confirm('Delete this template?')) return;

    try {
        const res = await fetch(`/api/institutions/${institutionId}/batch-templates/${id}`, { method: 'DELETE' });
        if (!res.ok) throw new Error('Failed to delete');
        loadTemplates();
        window.toast?.success('Template deleted');
    } catch (err) {
        window.toast?.error(err.message);
    }
}

// Stats
async function loadStats() {
    try {
        const res = await fetch(`/api/institutions/${institutionId}/batches/stats`);
        if (!res.ok) throw new Error('Failed to load stats');
        const stats = await res.json();

        document.getElementById('totalBatches').textContent = stats.total_batches || 0;
        document.getElementById('totalDocs').textContent = stats.total_documents_processed || 0;
        document.getElementById('totalCost').textContent = `$${(stats.total_cost || 0).toFixed(2)}`;
        document.getElementById('avgSuccess').textContent = `${(stats.avg_success_rate || 0).toFixed(1)}%`;
    } catch (err) {
        console.error('Failed to load stats:', err);
        document.getElementById('totalBatches').textContent = '0';
        document.getElementById('totalDocs').textContent = '0';
        document.getElementById('totalCost').textContent = '$0.00';
        document.getElementById('avgSuccess').textContent = '0%';
    }
}

// Batch List
async function loadBatches(typeFilter = '') {
    try {
        const url = `/api/institutions/${institutionId}/batches?limit=20&operation_type=${typeFilter}`;
        const res = await fetch(url);
        if (!res.ok) throw new Error('Failed to load batches');
        const data = await res.json();

        renderBatchList(data.batches || []);
    } catch (err) {
        console.error('Failed to load batches:', err);
        renderBatchList([]);
    }
}

function renderBatchList(batches) {
    const tbody = document.getElementById('batchList');

    if (batches.length === 0) {
        tbody.innerHTML = `
            <tr>
                <td colspan="9" class="text-center text-muted" style="padding: var(--spacing-xl);">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 64px; height: 64px; color: var(--text-muted); margin: 0 auto var(--spacing-lg);">
                        <path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                    </svg>
                    <p style="font-size: 1.1em;">No batch operations yet</p>
                    <p style="color: var(--text-muted);">Batch operations will appear here after running bulk audits or remediations.</p>
                </td>
            </tr>
        `;
        return;
    }

    tbody.innerHTML = batches.map(batch => {
        const date = new Date(batch.created_at).toLocaleString();
        const duration = batch.completed_at && batch.created_at
            ? formatDuration(new Date(batch.completed_at) - new Date(batch.created_at))
            : '--';
        const successRate = batch.document_count > 0
            ? ((batch.completed_count / batch.document_count) * 100).toFixed(1)
            : 0;
        const priorityLevel = batch.priority_level || 3;
        const priorityName = priorityNames[priorityLevel] || 'normal';

        return `
            <tr onclick="showBatchDetail('${batch.id}')">
                <td>${date}</td>
                <td>
                    <span class="batch-type-badge badge-${batch.operation_type}">${batch.operation_type}</span>
                </td>
                <td>
                    <span class="priority-badge priority-${priorityName}">${priorityName}</span>
                </td>
                <td>${batch.document_count}</td>
                <td>
                    <span class="${successRate >= 80 ? 'text-success' : successRate >= 50 ? 'text-warning' : 'text-muted'}">
                        ${successRate}%
                    </span>
                </td>
                <td>$${(batch.actual_cost || batch.estimated_cost || 0).toFixed(2)}</td>
                <td>${duration}</td>
                <td>
                    <span class="badge badge-${batch.status === 'completed' ? 'success' : batch.status === 'failed' ? 'danger' : 'warning'}">
                        ${batch.status}
                    </span>
                </td>
                <td>
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 16px; height: 16px;">
                        <path d="M9 18l6-6-6-6"/>
                    </svg>
                </td>
            </tr>
        `;
    }).join('');
}

async function showBatchDetail(batchId) {
    try {
        const res = await fetch(`/api/institutions/${institutionId}/batches/${batchId}`);
        if (!res.ok) throw new Error('Failed to load batch details');
        const batch = await res.json();

        document.getElementById('detailTitle').textContent = `Batch ${batchId.slice(0, 12)}... Details`;

        const successRate = batch.success_rate || 0;
        const totalDuration = formatDuration(batch.total_duration_ms);
        const priorityLevel = batch.priority_level || 3;
        const priorityName = priorityNames[priorityLevel] || 'normal';

        const detailHtml = `
            <div class="summary-section">
                <h4>Overview</h4>
                <div class="summary-grid">
                    <div class="summary-item">
                        <span class="summary-label">Operation Type</span>
                        <span class="summary-value">${batch.operation_type}</span>
                    </div>
                    <div class="summary-item">
                        <span class="summary-label">Priority</span>
                        <span class="summary-value priority-badge priority-${priorityName}">${priorityName}</span>
                    </div>
                    <div class="summary-item">
                        <span class="summary-label">Status</span>
                        <span class="summary-value badge badge-${batch.status === 'completed' ? 'success' : batch.status === 'failed' ? 'danger' : 'warning'}">${batch.status}</span>
                    </div>
                    <div class="summary-item">
                        <span class="summary-label">Total Documents</span>
                        <span class="summary-value">${batch.document_count}</span>
                    </div>
                    <div class="summary-item">
                        <span class="summary-label">Success Rate</span>
                        <span class="summary-value">${successRate.toFixed(1)}%</span>
                    </div>
                    <div class="summary-item">
                        <span class="summary-label">Total Duration</span>
                        <span class="summary-value">${totalDuration}</span>
                    </div>
                </div>
            </div>

            ${batch.status === 'pending' || batch.status === 'running' ? `
            <div class="summary-section">
                <h4>Change Priority</h4>
                <div style="display: flex; gap: var(--spacing-sm);">
                    <button class="btn btn-sm ${priorityLevel === 1 ? 'btn-primary' : 'btn-secondary'}" onclick="changePriority('${batchId}', 1)">Critical</button>
                    <button class="btn btn-sm ${priorityLevel === 2 ? 'btn-primary' : 'btn-secondary'}" onclick="changePriority('${batchId}', 2)">High</button>
                    <button class="btn btn-sm ${priorityLevel === 3 ? 'btn-primary' : 'btn-secondary'}" onclick="changePriority('${batchId}', 3)">Normal</button>
                    <button class="btn btn-sm ${priorityLevel === 4 ? 'btn-primary' : 'btn-secondary'}" onclick="changePriority('${batchId}', 4)">Low</button>
                </div>
            </div>
            ` : ''}

            <div class="summary-section">
                <h4>Items (${batch.items ? batch.items.length : 0})</h4>
                <div class="item-list">
                    ${batch.items && batch.items.length > 0 ? batch.items.map(item => `
                        <div class="item-row">
                            <div class="item-name">${escapeHtml(item.document_name || item.document_id)}</div>
                            <div class="item-stats">
                                <span class="badge badge-${item.status === 'completed' ? 'success' : item.status === 'failed' ? 'danger' : 'warning'}">
                                    ${item.status}
                                </span>
                                ${item.findings_count !== undefined ? `<span>${item.findings_count} findings</span>` : ''}
                                <span>${formatDuration(item.duration_ms)}</span>
                            </div>
                        </div>
                    `).join('') : '<p class="text-muted text-center">No items</p>'}
                </div>
            </div>
        `;

        document.getElementById('batchDetailContent').innerHTML = detailHtml;
        document.getElementById('batchDetailModal').classList.remove('hidden');
    } catch (err) {
        console.error('Failed to load batch detail:', err);
        window.toast?.error('Failed to load batch details');
    }
}

async function changePriority(batchId, level) {
    try {
        const res = await fetch(`/api/institutions/${institutionId}/batches/${batchId}/priority`, {
            method: 'PATCH',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ priority_level: level }),
        });

        if (!res.ok) {
            const err = await res.json();
            throw new Error(err.error || 'Failed to update priority');
        }

        closeDetailModal();
        loadQueueStatus();
        loadBatches();
        window.toast?.success(`Priority updated to ${priorityNames[level]}`);
    } catch (err) {
        window.toast?.error(err.message);
    }
}

function closeDetailModal() {
    document.getElementById('batchDetailModal').classList.add('hidden');
}

function formatDuration(ms) {
    if (!ms) return '--';
    const seconds = Math.floor(ms / 1000);
    const minutes = Math.floor(seconds / 60);
    const hours = Math.floor(minutes / 60);

    if (hours > 0) {
        return `${hours}h ${minutes % 60}m`;
    } else if (minutes > 0) {
        return `${minutes}m ${seconds % 60}s`;
    } else {
        return `${seconds}s`;
    }
}

function escapeHtml(text) {
    if (!text) return '';
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}

// Event listeners
document.getElementById('typeFilter').addEventListener('change', (e) => {
    loadBatches(e.target.value);
});

// Load on page load and set refresh interval
document.addEventListener('DOMContentLoaded', () => {
    loadQueueStatus();
    loadStats();
    loadTemplates();
    loadBatches();

    // Refresh queue status every 10 seconds
    setInterval(loadQueueStatus, 10000);
});
</script>
{% endblock %}
```
  </action>
  <verify>Test template exists: python -c "from pathlib import Path; p = Path('templates/institutions/batch_history.html'); assert p.exists() and 'Queue Monitor' in p.read_text(); print('Template OK')"</verify>
  <done>batch_history.html enhanced with queue monitor, templates section, and priority badges</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run migration: `flask db upgrade`
2. Test priority in batch creation (update test to include priority)
3. Test priority change endpoint: `curl -X PATCH /api/institutions/{id}/batches/{id}/priority -d '{"priority_level": 1}'`
4. Test UI: Visit `/institutions/{id}/batch-history` and verify:
   - Queue monitor shows real-time counts
   - Templates section allows create/execute/delete
   - Batch list shows priority badges
   - Detail modal allows priority change for pending batches
5. Verify app starts: `python app.py` (no import errors)
</verification>

<success_criteria>
- priority_level column added to batch_operations via migration 0042
- BatchService.create_batch accepts priority_level parameter
- BatchService.update_priority method works for pending/running batches
- Priority change API endpoint works
- Queue status endpoint includes priority in active batches
- Batch history page shows:
  - Queue monitor card with real-time stats
  - Templates section with CRUD
  - Priority badges in batch table
  - Priority selector in batch detail modal
</success_criteria>

<output>
After completion, create `.planning/phases/9-05-enhanced-batch-processing/9-05-03-SUMMARY.md`
</output>
