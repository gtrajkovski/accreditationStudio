# Plan 37-01: Federal Regulations Library

## Objective

Create structured federal regulation bundles with applicability rules that can be queried based on institution profile.

## Directory Structure

```
src/regulatory/
├── __init__.py
├── federal/
│   ├── __init__.py
│   ├── bundles.py          # Bundle loading and applicability
│   ├── models.py           # Dataclasses
│   ├── title_iv.json       # Student financial aid
│   ├── ferpa.json          # Student records privacy
│   ├── title_ix.json       # Sex discrimination
│   ├── clery.json          # Campus safety
│   ├── ada.json            # Accessibility
│   ├── gainful_employment.json
│   └── coppa.json          # Children's privacy
```

## Models

File: `src/regulatory/federal/models.py`

```python
from dataclasses import dataclass, field
from typing import List, Dict, Any, Optional


@dataclass
class FederalRequirement:
    """A specific federal requirement within a bundle."""
    id: str
    citation: str           # e.g., "34 CFR 668.43(a)(1)"
    title: str
    description: str
    evidence_types: List[str] = field(default_factory=list)
    common_violations: List[str] = field(default_factory=list)
    penalty_range: str = ""
    effective_date: str = ""

    def to_dict(self) -> Dict[str, Any]:
        return {
            "id": self.id,
            "citation": self.citation,
            "title": self.title,
            "description": self.description,
            "evidence_types": self.evidence_types,
            "common_violations": self.common_violations,
            "penalty_range": self.penalty_range,
            "effective_date": self.effective_date
        }


@dataclass
class FederalBundle:
    """A federal regulation bundle with applicability rules."""
    id: str
    name: str
    short_name: str
    description: str
    citations: List[str] = field(default_factory=list)
    applicability_rule: str = ""  # Python expression
    requirements: List[FederalRequirement] = field(default_factory=list)
    last_updated: str = ""
    effective_date: str = ""
    enforcement_agency: str = ""
    penalty_authority: str = ""

    def to_dict(self) -> Dict[str, Any]:
        return {
            "id": self.id,
            "name": self.name,
            "short_name": self.short_name,
            "description": self.description,
            "citations": self.citations,
            "applicability_rule": self.applicability_rule,
            "requirements": [r.to_dict() for r in self.requirements],
            "last_updated": self.last_updated,
            "effective_date": self.effective_date,
            "enforcement_agency": self.enforcement_agency,
            "penalty_authority": self.penalty_authority,
            "requirement_count": len(self.requirements)
        }

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "FederalBundle":
        requirements = [
            FederalRequirement(**r) for r in data.get("requirements", [])
        ]
        return cls(
            id=data["id"],
            name=data["name"],
            short_name=data["short_name"],
            description=data.get("description", ""),
            citations=data.get("citations", []),
            applicability_rule=data.get("applicability_rule", ""),
            requirements=requirements,
            last_updated=data.get("last_updated", ""),
            effective_date=data.get("effective_date", ""),
            enforcement_agency=data.get("enforcement_agency", ""),
            penalty_authority=data.get("penalty_authority", "")
        )
```

## Bundle JSON Files

File: `src/regulatory/federal/title_iv.json`

```json
{
  "id": "title_iv",
  "name": "Title IV Student Financial Aid",
  "short_name": "Title IV",
  "description": "Requirements for institutions participating in federal student financial aid programs",
  "citations": ["34 CFR Part 600", "34 CFR Part 668", "34 CFR Part 674", "34 CFR Part 682", "34 CFR Part 685"],
  "applicability_rule": "institution.title_iv_eligible == True",
  "enforcement_agency": "U.S. Department of Education",
  "penalty_authority": "Fine and suspension from Title IV programs",
  "last_updated": "2024-07-01",
  "effective_date": "1965-11-08",
  "requirements": [
    {
      "id": "t4-001",
      "citation": "34 CFR 668.43(a)(1)",
      "title": "Consumer Information Disclosure",
      "description": "Institution must make available to enrolled and prospective students information about student body diversity, retention rates, and graduation rates",
      "evidence_types": ["catalog", "website", "consumer_disclosure"],
      "common_violations": ["Missing graduation rates", "Outdated diversity statistics"],
      "penalty_range": "Fine up to $62,689 per violation"
    },
    {
      "id": "t4-002",
      "citation": "34 CFR 668.43(a)(5)",
      "title": "Refund Policy Disclosure",
      "description": "Institution must disclose refund policies for returning unearned tuition, fees, and room and board",
      "evidence_types": ["catalog", "enrollment_agreement", "refund_policy"],
      "common_violations": ["Policy not clearly stated", "Missing timeframes"],
      "penalty_range": "Fine up to $62,689 per violation"
    },
    {
      "id": "t4-003",
      "citation": "34 CFR 668.23",
      "title": "Administrative Capability",
      "description": "Institution must demonstrate administrative capability including adequate staffing and record-keeping",
      "evidence_types": ["organizational_chart", "staff_credentials", "policies"],
      "common_violations": ["Insufficient staffing", "Inadequate record retention"],
      "penalty_range": "Loss of Title IV eligibility"
    }
  ]
}
```

File: `src/regulatory/federal/ferpa.json`

```json
{
  "id": "ferpa",
  "name": "Family Educational Rights and Privacy Act",
  "short_name": "FERPA",
  "description": "Protects the privacy of student education records",
  "citations": ["20 U.S.C. § 1232g", "34 CFR Part 99"],
  "applicability_rule": "True",
  "enforcement_agency": "U.S. Department of Education, Student Privacy Policy Office",
  "penalty_authority": "Withdrawal of federal funding",
  "last_updated": "2024-01-15",
  "effective_date": "1974-08-21",
  "requirements": [
    {
      "id": "ferpa-001",
      "citation": "34 CFR 99.7",
      "title": "Annual Notification",
      "description": "Institution must annually notify students of their rights under FERPA",
      "evidence_types": ["catalog", "student_handbook", "website"],
      "common_violations": ["No annual notification", "Incomplete rights listing"],
      "penalty_range": "Loss of federal funding"
    },
    {
      "id": "ferpa-002",
      "citation": "34 CFR 99.31",
      "title": "Disclosure Restrictions",
      "description": "Institution may not disclose personally identifiable information without consent except in specified circumstances",
      "evidence_types": ["privacy_policy", "release_forms", "staff_training"],
      "common_violations": ["Unauthorized disclosure", "Missing consent forms"],
      "penalty_range": "Loss of federal funding"
    }
  ]
}
```

File: `src/regulatory/federal/clery.json`

```json
{
  "id": "clery",
  "name": "Jeanne Clery Disclosure of Campus Security Policy and Campus Crime Statistics Act",
  "short_name": "Clery Act",
  "description": "Requires disclosure of campus crime statistics and security policies",
  "citations": ["20 U.S.C. § 1092(f)", "34 CFR 668.46"],
  "applicability_rule": "institution.title_iv_eligible == True",
  "enforcement_agency": "U.S. Department of Education",
  "penalty_authority": "Fine up to $62,689 per violation",
  "last_updated": "2024-01-01",
  "effective_date": "1990-11-08",
  "requirements": [
    {
      "id": "clery-001",
      "citation": "34 CFR 668.46(b)",
      "title": "Annual Security Report",
      "description": "Institution must publish an annual security report containing crime statistics for the previous three years",
      "evidence_types": ["annual_security_report", "crime_log"],
      "common_violations": ["Late publication", "Missing crime categories"],
      "penalty_range": "Fine up to $62,689 per violation"
    }
  ]
}
```

(Create similar JSON files for title_ix.json, ada.json, gainful_employment.json, coppa.json)

## Bundle Service

File: `src/regulatory/federal/bundles.py`

```python
import json
from pathlib import Path
from typing import List, Dict, Any, Optional

from src.regulatory.federal.models import FederalBundle


class FederalBundleService:
    """Service for loading and querying federal regulation bundles."""

    _bundles: Dict[str, FederalBundle] = {}
    _loaded: bool = False

    @classmethod
    def _load_bundles(cls) -> None:
        """Load all bundle JSON files."""
        if cls._loaded:
            return

        bundle_dir = Path(__file__).parent
        for json_file in bundle_dir.glob("*.json"):
            with open(json_file) as f:
                data = json.load(f)
                bundle = FederalBundle.from_dict(data)
                cls._bundles[bundle.id] = bundle

        cls._loaded = True

    @classmethod
    def list_bundles(cls) -> List[Dict[str, Any]]:
        """Return all bundles as summary dicts."""
        cls._load_bundles()
        return [
            {
                "id": b.id,
                "name": b.name,
                "short_name": b.short_name,
                "requirement_count": len(b.requirements),
                "applicability_rule": b.applicability_rule
            }
            for b in cls._bundles.values()
        ]

    @classmethod
    def get_bundle(cls, bundle_id: str) -> Optional[FederalBundle]:
        """Get full bundle by ID."""
        cls._load_bundles()
        return cls._bundles.get(bundle_id)

    @classmethod
    def get_applicable_bundles(cls, institution_profile: Dict[str, Any]) -> List[FederalBundle]:
        """Return bundles applicable to institution based on profile."""
        cls._load_bundles()
        applicable = []

        for bundle in cls._bundles.values():
            if cls._evaluate_applicability(bundle.applicability_rule, institution_profile):
                applicable.append(bundle)

        return applicable

    @classmethod
    def _evaluate_applicability(cls, rule: str, profile: Dict[str, Any]) -> bool:
        """Safely evaluate applicability rule against profile."""
        if not rule or rule == "True":
            return True

        # Create safe evaluation context
        context = {
            "institution": type("Institution", (), profile)()
        }

        try:
            return eval(rule, {"__builtins__": {}}, context)
        except Exception:
            return False

    @classmethod
    def search_requirements(cls, query: str) -> List[Dict[str, Any]]:
        """Search across all bundles for matching requirements."""
        cls._load_bundles()
        results = []
        query_lower = query.lower()

        for bundle in cls._bundles.values():
            for req in bundle.requirements:
                if (query_lower in req.title.lower() or
                    query_lower in req.description.lower() or
                    query_lower in req.citation.lower()):
                    results.append({
                        "bundle_id": bundle.id,
                        "bundle_name": bundle.short_name,
                        "requirement": req.to_dict()
                    })

        return results
```

## API Blueprint

File: `src/api/federal.py`

```python
from flask import Blueprint, jsonify, request
from src.regulatory.federal.bundles import FederalBundleService
from src.core.workspace import WorkspaceManager

federal_bp = Blueprint("federal", __name__, url_prefix="/api/federal")

_workspace_manager = None


def init_federal_bp(workspace_manager: WorkspaceManager):
    global _workspace_manager
    _workspace_manager = workspace_manager


@federal_bp.route("/bundles", methods=["GET"])
def list_bundles():
    """List all federal regulation bundles."""
    bundles = FederalBundleService.list_bundles()
    return jsonify({"bundles": bundles})


@federal_bp.route("/bundles/<bundle_id>", methods=["GET"])
def get_bundle(bundle_id: str):
    """Get full bundle details."""
    bundle = FederalBundleService.get_bundle(bundle_id)
    if not bundle:
        return jsonify({"error": "Bundle not found"}), 404
    return jsonify(bundle.to_dict())


@federal_bp.route("/applicable/<institution_id>", methods=["GET"])
def get_applicable_bundles(institution_id: str):
    """Get bundles applicable to institution."""
    institution = _workspace_manager.load_institution(institution_id)
    if not institution:
        return jsonify({"error": "Institution not found"}), 404

    profile = {
        "title_iv_eligible": getattr(institution, "title_iv_eligible", True),
        "modality": getattr(institution, "modality", "ground"),
        "serves_minors": getattr(institution, "serves_minors", False),
        "for_profit": getattr(institution, "for_profit", False),
        "offers_certificates": getattr(institution, "offers_certificates", False)
    }

    bundles = FederalBundleService.get_applicable_bundles(profile)
    return jsonify({
        "institution_id": institution_id,
        "applicable_bundles": [b.to_dict() for b in bundles]
    })


@federal_bp.route("/search", methods=["GET"])
def search_requirements():
    """Search across all bundles."""
    query = request.args.get("q", "")
    if not query:
        return jsonify({"error": "Query required"}), 400

    results = FederalBundleService.search_requirements(query)
    return jsonify({"query": query, "results": results, "count": len(results)})
```

## Tests

File: `tests/test_federal_bundles.py`

```python
import pytest
from src.regulatory.federal.bundles import FederalBundleService


def test_list_bundles_returns_all():
    """Verify all bundles are loaded."""
    bundles = FederalBundleService.list_bundles()
    assert len(bundles) >= 5
    ids = [b["id"] for b in bundles]
    assert "title_iv" in ids
    assert "ferpa" in ids


def test_get_bundle_returns_requirements():
    """Verify bundle has requirements."""
    bundle = FederalBundleService.get_bundle("title_iv")
    assert bundle is not None
    assert len(bundle.requirements) >= 1
    assert bundle.requirements[0].citation.startswith("34 CFR")


def test_applicability_title_iv():
    """Verify Title IV applicability rule."""
    profile = {"title_iv_eligible": True}
    bundles = FederalBundleService.get_applicable_bundles(profile)
    bundle_ids = [b.id for b in bundles]
    assert "title_iv" in bundle_ids

    profile_no = {"title_iv_eligible": False}
    bundles_no = FederalBundleService.get_applicable_bundles(profile_no)
    bundle_ids_no = [b.id for b in bundles_no]
    assert "title_iv" not in bundle_ids_no


def test_ferpa_always_applicable():
    """Verify FERPA applies to all institutions."""
    profile = {"title_iv_eligible": False}
    bundles = FederalBundleService.get_applicable_bundles(profile)
    bundle_ids = [b.id for b in bundles]
    assert "ferpa" in bundle_ids


def test_search_requirements():
    """Verify search works."""
    results = FederalBundleService.search_requirements("refund")
    assert len(results) >= 1
    assert any("refund" in r["requirement"]["title"].lower() for r in results)
```

## App Integration

Add to `app.py`:

```python
from src.api.federal import federal_bp, init_federal_bp

init_federal_bp(workspace_manager)
app.register_blueprint(federal_bp)
```

## Acceptance Criteria

- [ ] 7 federal bundle JSON files created with requirements
- [ ] Bundle service loads and queries bundles
- [ ] Applicability rules evaluate against institution profile
- [ ] API returns applicable bundles for institution
- [ ] Search works across all bundles
- [ ] All tests pass
