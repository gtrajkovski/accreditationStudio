# Plan 43-01: Activity Audit Trail

## Objective

Implement user-facing activity logging for all significant actions, with filtering, pagination, and export.

## Dependencies

- Phase 42 (RBAC) must be complete

## Tasks

### Task 1: Database Migration

**File:** `src/db/migrations/0048_activity_log.sql`

```sql
CREATE TABLE IF NOT EXISTS activity_log (
    id TEXT PRIMARY KEY,
    user_id TEXT,
    user_name TEXT,
    institution_id TEXT,
    action TEXT NOT NULL,
    entity_type TEXT,
    entity_id TEXT,
    details TEXT,
    ip_address TEXT,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (institution_id) REFERENCES institutions(id)
);

CREATE INDEX IF NOT EXISTS idx_activity_user ON activity_log(user_id);
CREATE INDEX IF NOT EXISTS idx_activity_institution ON activity_log(institution_id);
CREATE INDEX IF NOT EXISTS idx_activity_action ON activity_log(action);
CREATE INDEX IF NOT EXISTS idx_activity_created ON activity_log(created_at);
CREATE INDEX IF NOT EXISTS idx_activity_entity ON activity_log(entity_type, entity_id);
```

**Commit:** `chore(43): add activity_log table`

### Task 2: Activity Service

**File:** `src/services/activity_service.py`

Action types:
- `user.login`, `user.logout`, `user.register`, `user.invite`, `user.role_change`
- `document.upload`, `document.delete`
- `audit.start`, `audit.complete`
- `remediation.start`, `remediation.approve`, `remediation.reject`
- `packet.create`, `packet.export`
- `finding.create`, `finding.resolve`
- `task.assign`, `task.complete`
- `settings.change`
- `institution.create`, `institution.update`

Implement:
- `log_activity(user_id, institution_id, action, entity_type=None, entity_id=None, details=None, ip_address=None)`
- `get_activity(institution_id, filters=None, page=1, per_page=50)` → paginated
- `get_activity_for_entity(entity_type, entity_id)` → list
- `get_activity_summary(institution_id, days=30)` → counts by action
- `export_activity(institution_id, start_date, end_date)` → CSV string

**Commit:** `feat(43): implement activity service`

### Task 3: Activity API Blueprint

**File:** `src/api/activity.py`

Blueprint: `activity_bp`, prefix `/api/activity`

Endpoints:
- `GET /` — paginated log with filters (compliance_officer+)
- `GET /summary` — stats (admin+)
- `GET /export` — CSV (admin+)
- `GET /entity/<type>/<id>` — entity history (compliance_officer+)

**Commit:** `feat(43): add activity API blueprint`

### Task 4: Activity Log UI

**File:** `templates/admin/activity.html`

- Table: timestamp, user, action, entity, details
- Filter bar: date range, user dropdown, action type
- Auto-refresh (30s) or manual refresh button
- Export CSV button
- Route: `/admin/activity` (compliance_officer+)

**Commit:** `feat(43): add activity log UI`

### Task 5: Integrate Logging

Add `activity_service.log_activity(...)` to existing blueprints:
- `documents_bp`: upload, delete
- `audits_bp`: start, complete
- `remediation_bp`: start, approve, reject
- `packets_bp`: create, export
- `auth_bp`: login, logout, register
- `users_bp`: invite, role change, remove
- `institutions_bp`: create, update
- `settings_bp`: any change

Pattern:
```python
activity_service.log_activity(
    user_id=g.current_user['id'],
    institution_id=institution_id,
    action='document.upload',
    entity_type='document',
    entity_id=doc_id,
    details=f"Uploaded {filename}"
)
```

**Commit:** `feat(43): integrate activity logging into blueprints`

### Task 6: Tests

**File:** `tests/test_activity.py`

Test:
- Activity logged on document upload
- Activity logged on audit start
- Activity logged on login
- Pagination works
- Filters work (date, action, user)
- Export generates valid CSV
- Scoped to institution

**Commit:** `test(43): add activity tests`

## Acceptance Criteria

```bash
pytest tests/test_activity.py -v
# All pass

# Verify end-to-end:
# 1. Login → activity log shows login event
# 2. Upload document → activity log shows upload event
```

## Final Commit

```
feat(phase-43): activity audit trail

- Add activity_log table (migration 0048)
- Implement activity_service with logging, querying, export
- Add activity API blueprint with 4 endpoints
- Add activity log UI page with filters and export
- Integrate activity logging into 10+ existing blueprints
- Tests covering logging, filtering, pagination, export

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
```
