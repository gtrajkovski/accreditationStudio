---
phase: post-v1.2-fixes
task: UI bug fixes
status: in_progress
last_updated: 2026-03-18T14:31:23.988Z
---

<current_state>
Fixing UI bugs discovered after v1.2.0 release. Several modal and styling issues identified and partially fixed.
</current_state>

<completed_work>

## Modal z-index fixes (modals appearing behind sidebar)
- Fixed z-index in 9 template files (changed from 1000 to 1300+)
- Files: documents.html, workbench.html, overview.html, impact.html, knowledge_graph.html, batch_history.html, coverage_map.html, compliance_heatmap.html, glossary.html
- Also fixed batch.css

## Sidebar collapse text hiding
- Added `font-size: 0` + `overflow: hidden` to collapsed nav items
- SVGs preserved with `font-size: initial`
- Fixed in main.css

## Readiness score circle size
- Reduced from 120px to 80px
- Added explicit width/height to SVG element
- Reduced font sizes for score number/label

## Stat icon size
- Reduced stat-icon from 48px to 36px container
- Reduced SVG from 24px to 18px

</completed_work>

<remaining_work>

## Program edit modal - NEEDS TESTING
- Modal z-index set to 9999
- Backdrop changed to position: fixed
- Modal content given z-index: 1
- User reported modal still not appearing - needs verification

## Alert icon sizes
- Added width="20" height="20" to dynamically rendered alert icons in JavaScript

## Other potential issues
- User mentioned "sessions details coming soon" message - not found in codebase, may be dynamic
- May need to check other modals across the app for similar issues

</remaining_work>

<decisions_made>

- Use CSS variable fallback pattern: `z-index: var(--z-modal, 1300)` for consistency
- Increased program modal z-index to 9999 to ensure it's above everything
- Changed modal-backdrop from `position: absolute` to `position: fixed` for full viewport coverage
- Used `font-size: 0` trick to hide nav text when sidebar collapsed (avoids wrapping text in spans)

</decisions_made>

<blockers>

- Program edit modal may still not be appearing correctly - user reported issue persists
- Need user to hard refresh (Ctrl+F5) and test again

</blockers>

<context>

Working on post-v1.2 polish. User found multiple UI issues:
1. Modals opening behind sidebar (z-index 1000 < sidebar 1100)
2. Sidebar collapse not hiding text properly
3. Large icons (readiness circle, stat icons)
4. Program edit modal not accessible

All fixes are uncommitted. App was running on port 5006.

</context>

<next_action>

1. Commit all current UI fixes
2. Test program edit modal - may need additional fixes
3. Search for any remaining z-index: 1000 instances
4. Verify sidebar collapse works in all browsers

To resume: `/gsd:resume-work`

</next_action>
