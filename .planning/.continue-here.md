---
phase: tech-debt
task: 4
total_tasks: 4
status: paused
last_updated: 2026-03-27T17:05:50.133Z
---

<current_state>
Tech debt cleanup 3/4 complete. TD-1, TD-2, TD-3 all committed. Ready to start TD-4 (workspace file I/O caching).
</current_state>

<completed_work>

- TD-1: Split models.py into domain modules - Done (commit 4bce6f0)
  - Split 2706-line models.py into 15 domain modules under src/core/models/
  - __init__.py re-exports everything for backward compatibility
  - models_backup.py preserved for rollback

- TD-2: Silent exception handler sweep - Done (commit d53d413)
  - Added logging to 38 silent `except Exception: pass` handlers
  - 21 files modified across api/, services/, agents/, core/, importers/
  - Pattern: `except Exception as e: logger.debug("context: %s", e)`

- TD-3: AgentType enum cleanup - Done (commit 7384f22)
  - Removed 15 unused AgentType values (never implemented)
  - Kept 29 registered agent types
  - Fixed registry: added missing packet_agent and findings_agent imports
  - All 35 agent tests pass
</completed_work>

<remaining_work>

- TD-4: Workspace file I/O caching - Not started
  - Goal: Reduce redundant file reads in WorkspaceManager
  - Scope: Add in-memory caching with TTL for frequently accessed files
  - Files: src/core/workspace.py (WorkspaceManager class)
</remaining_work>

<decisions_made>

- Keep models_backup.py as rollback safety
- Use logger.debug for expected failures (avoid log noise)
- Keep re-raise handlers unchanged (ai/client.py pattern is correct)
- Remove unused AgentType values rather than keeping placeholders
- Add missing registry imports for packet_agent and findings_agent
</decisions_made>

<blockers>
None
</blockers>

<context>
Working through tech debt tasks before Phase 9 features. The models.py split (TD-1) was the largest change. Exception handlers (TD-2) were straightforward pattern replacement. AgentType cleanup (TD-3) required careful analysis of which types were actually registered vs planned-but-never-implemented.

TD-4 workspace caching will need analysis of WorkspaceManager read patterns to determine which files benefit most from caching.
</context>

<next_action>
Start TD-4: Analyze WorkspaceManager in src/core/workspace.py for file read patterns, then implement LRU cache with TTL for institution.json and truth_index.json which are read frequently.
</next_action>
